% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sign.R
\name{sign_pdf}
\alias{sign_pdf}
\title{Digitally sign a PDF document}
\usage{
sign_pdf(
  pdf_file,
  output_file,
  keystore_path = Sys.getenv("KEYSTORE_PATH"),
  keystore_password = Sys.getenv("KEY_PASSWORD"),
  signtext = NULL,
  validate_link = NULL,
  reason = NULL,
  signer_name = NULL,
  page = 1,
  x = 36,
  y = 36,
  width = 320,
  height = 64,
  font_size = 8,
  font = NULL,
  image = NULL,
  border = TRUE,
  translate = FALSE,
  tsa_url = NULL,
  pades_level = c("bb", "bt", "blt", "blta")
)
}
\arguments{
\item{pdf_file}{Path to the input PDF.}

\item{output_file}{Path where the signed PDF is written.}

\item{keystore_path}{Path to the \code{.p12}/\code{.pfx} keystore. Defaults to the
\code{KEYSTORE_PATH} environment variable.}

\item{keystore_password}{Password for the keystore. Defaults to the
\code{KEY_PASSWORD} environment variable.}

\item{signtext}{Optional text for a \emph{visible} signature box. When \code{NULL} or
empty the signature is invisible.}

\item{validate_link}{Optional validation URL appended to the visible box.}

\item{reason, signer_name}{Optional \verb{/Reason} and \verb{/Name} for the signature
dictionary.}

\item{page}{1-based page number for the visible box.}

\item{x, y, width, height}{Visible box geometry, in PDF points (origin at the
page's bottom-left).}

\item{font_size}{Font size for the visible box, in points.}

\item{font}{Optional path to a TrueType/OpenType font file (\code{.ttf}/\code{.otf}) to
embed in the visible box. When \code{NULL}, the standard Helvetica is used. Only
the WinAnsi (Latin-1) glyph range is embedded. Ignored for invisible
signatures.}

\item{image}{Optional path to a PNG or JPEG logo drawn in the visible box.
Ignored for invisible signatures.}

\item{border}{Draw a border around the visible box.}

\item{translate}{If \code{TRUE}, the date label in the visible box is in
Portuguese; otherwise English.}

\item{tsa_url}{Optional RFC 3161 Time-Stamping Authority \verb{http://} URL.
Required for \code{pades_level} \code{"bt"} and above. Requires network access.}

\item{pades_level}{PAdES conformance level: \code{"bb"} (baseline, default),
\code{"bt"} (+ signature timestamp), \code{"blt"} (+ DSS with certificates and CRLs),
or \code{"blta"} (+ a document timestamp over the whole file). Levels \code{"bt"} and
above need \code{tsa_url}.}
}
\value{
Invisibly, the path to the signed PDF. Raises an error on failure.
}
\description{
Signs \code{pdf_file} using an RSA key + certificate stored in a PKCS#12
(\code{.p12}/\code{.pfx}) keystore, writing the signed document to \code{output_file}. The
signature is a detached \code{adbe.pkcs7.detached} CMS over the whole document and
is added as an incremental update, so any pre-existing signatures stay valid.
}
\examples{
\dontrun{
sign_pdf(
  pdf_file = "input.pdf",
  output_file = "signed.pdf",
  keystore_path = "keystore.p12",
  keystore_password = "password",
  signtext = "Document digitally signed by CastLab",
  validate_link = "https://castlab.org/validate",
  translate = TRUE
)
}
}
