
# Export all names
exportPattern(".")

import(lattice, mvtnorm, coda, pscl, MCMCpack)

# Import all packages listed as Imports or Depends
importFrom("stats", "model.frame", "model.matrix", "na.action",
             "na.omit", "rgamma", "rnorm", "runif")
