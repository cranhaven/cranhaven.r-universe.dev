% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testeCorrelacao.R
\name{teste.correlacao}
\alias{teste.correlacao}
\title{Teste de correlacao (Pearson, Spearman ou Kendall)}
\usage{
teste.correlacao(
  x,
  y = NULL,
  metodo = "auto",
  estilo = 1,
  ajuda = FALSE,
  verbose = TRUE,
  plot_normalidade = FALSE
)
}
\arguments{
\item{x}{Vetor numerico ou data frame com duas colunas numericas}

\item{y}{Vetor numerico (opcional se x for data frame)}

\item{metodo}{Metodo de correlacao: "auto" (padrao), "pearson", "spearman" ou "kendall"}

\item{estilo}{Estetica do plot gerado pela funcao.}

\item{ajuda}{Se TRUE, exibe explicacao detalhada da funcao}

\item{verbose}{Se TRUE, imprime mensagens sobre o metodo escolhido e testes de normalidade}

\item{plot_normalidade}{Se TRUE, gera QQ-plots para avaliacao da normalidade dos dados}
}
\value{
Objeto de classe htest invisivel com o resultado do teste de correlacao
}
\description{
Realiza teste de correlacao entre duas variaveis numericas, escolhendo automaticamente
entre Pearson, Spearman ou Kendall com base na normalidade, empates e outliers.
Opcionalmente exibe graficos de diagnostico e grafico da correlacao com linha de tendencia.
}
\examples{
# Pearson
set.seed(123)
x <- rnorm(30, sd = 1)
y <- x + rnorm(30, sd = 1)
teste.correlacao(x, y)

# Spearman
set.seed(123)
x <- runif(300)
y <- log(x + 0.1) + rnorm(300, sd = 0.05)
teste.correlacao(x, y)

# Kendall
set.seed(123)
x  <- rnorm(200)
y  <- x + rnorm(200, sd = 0.2)
x_out <- runif(20, -10, 10)
y_out <- runif(20, -10, 10)
x <- c(x, x_out)
y <- c(y, y_out)
teste.correlacao(x, y)

}
