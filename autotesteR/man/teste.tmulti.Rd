% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testeTMulti.R
\name{teste.tmulti}
\alias{teste.tmulti}
\title{Testes multiplos entre grupos numericos (t ou Mann-Whitney), com diagnostico automatico e grafico anotado}
\usage{
teste.tmulti(
  ...,
  comparacoes = NULL,
  titulo = "Comparacoes multiplas (t / MW)",
  x = "",
  y = "Valor",
  estilo = 1,
  ajuda = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{...}{Data frame contendo apenas colunas numericas, ou varios vetores
numericos nomeados (cada vetor corresponde a um grupo).}

\item{comparacoes}{Formato flexivel. Pode ser:
\itemize{
\item \code{NULL} (padrao), testando todas as comparacoes 2 a 2;
\item \verb{list(c(\\"A\\",\\"B\\"), c(\\"C\\",\\"D\\"))};
\item vetor de nomes com tamanho par, ex.: \verb{c(\\"A\\",\\"B\\",\\"C\\",\\"D\\")};
\item par unico, ex.: \verb{c(\\"A\\",\\"B\\")};
\item string como \code{"A,B"} ou \code{"A - B"}.
}}

\item{titulo}{Titulo do grafico.}

\item{x}{Rotulo do eixo x.}

\item{y}{Rotulo do eixo y.}

\item{estilo}{Inteiro 1-4 definindo o estilo visual do grafico.
1 = boxplot;
2 = violino + boxplot;
3 = violino minimalista;
4 = half-eye (requer ggdist).}

\item{ajuda}{mostrar ajuda rapida}

\item{verbose}{Se \code{TRUE} (padrao), imprime mensagens e resumo estatistico.}
}
\value{
Uma lista invisivel com:
\itemize{
\item \code{resultados}: tibble com grupo1, grupo2, metodo (t/MW) e p-valor;
\item \code{pares_signif}: subconjunto significativo (p < 0.05);
\item \code{grafico}: objeto ggplot final;
\item \code{dados_long}: dados reorganizados em formato longo.
}
}
\description{
Executa comparacoes multiplas entre grupos numericos (como colunas de um
data frame ou vetores nomeados) usando teste t ou Mann-Whitney (MW),
escolhidos automaticamente com base na normalidade (Shapiro-Wilk) e
homogeneidade de variancias (teste de Levene).
}
\details{
A funcao tambem:
\itemize{
\item aceita diferentes formatos de entrada para definir as comparacoes
(lista de pares, vetores de nomes, strings, etc.);
\item imprime um resumo (medias/DP) e destaca comparacoes significativas;
\item gera graficos com barras de significancia e quatro estilos visuais personalizaveis;
\item retorna resultados completos para uso programatico.
}
}
\examples{
df <- data.frame(
  controle   = rnorm(30, 10),
  tratamento = rnorm(30, 12),
  teste1     = rnorm(30, 11),
  teste2     = rnorm(30, 15)
)

teste.tmulti(df)

teste.tmulti(df,
  comparacoes = list(c("controle", "tratamento"),
                     c("tratamento", "teste1"))
)

}
