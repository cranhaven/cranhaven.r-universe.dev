% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{nhanes_merge}
\alias{nhanes_merge}
\title{Merge NHANES component data frames by SEQN}
\usage{
nhanes_merge(..., by = "SEQN", type = c("inner", "left"), weight_var = NULL)
}
\arguments{
\item{...}{Two or more data frames from \code{\link[=nhanes_download]{nhanes_download()}}, each containing
a \code{SEQN} column. For multi-cycle datasets, also a \code{cycle} column.}

\item{by}{Character vector of join key(s). Default \code{"SEQN"}. For multi-cycle
data, use \code{c("SEQN", "cycle")}.}

\item{type}{Character. Join type: \code{"inner"} retains only participants present
in all files; \code{"left"} retains all participants from the first file.
Default \code{"inner"} (the standard NHANES analytic approach).}

\item{weight_var}{Character or \code{NULL}. If supplied, validates that this
weight column exists in the merged result.}
}
\value{
A merged data frame. Duplicate columns (present in more than one
input) are deduplicated, keeping the version from the first data frame
where the column appears, with a warning.
}
\description{
Joins two or more NHANES data frames on the \code{SEQN} respondent sequence
number. Validates that survey design variables (PSU, strata, weights) are
present and warns when merging across components that use different weight
variables.
}
\details{
\subsection{Weight guidance}{

The appropriate weight depends on which components are merged:
\itemize{
\item \strong{Demographics only} -> \code{WTINT2YR} (interview weight)
\item \strong{Any exam/lab component} -> \code{WTMEC2YR} (MEC exam weight)
\item \strong{Dietary 24-hr recall} -> \code{WTDRD1} or \code{WTDR2D}
\item \strong{Multi-cycle pooled} -> divide the 2-year weight by the number of cycles,
or use the 4-year combined weight where available
}

This function warns but does not enforce weight selection. Use
\code{\link[=nhanes_cycles]{nhanes_cycles()}} to look up available weight variable names per cycle.
}
}
\examples{
\donttest{
demo  <- nhanes_download("DEMO",  "2015-2016")
bpx   <- nhanes_download("BPX",   "2015-2016")
trigly <- nhanes_download("TRIGLY","2015-2016")

analytic <- nhanes_merge(demo, bpx, trigly)

# Multi-cycle: use nhanes_stack() before merging
demo_list <- nhanes_download("DEMO", c("2013-2014", "2015-2016"))
bpx_list  <- nhanes_download("BPX",  c("2013-2014", "2015-2016"))
demo_pool <- nhanes_stack(demo_list)
bpx_pool  <- nhanes_stack(bpx_list)
analytic  <- nhanes_merge(demo_pool, bpx_pool, by = c("SEQN", "cycle"))
}
}
\seealso{
\code{\link[=nhanes_stack]{nhanes_stack()}} to row-bind per-cycle lists before merging;
\code{\link[=nhanes_download]{nhanes_download()}} to obtain the component data frames;
\code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}} to append mortality follow-up after merging.
}
