% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mortality.R
\name{nhanes_mortality_download}
\alias{nhanes_mortality_download}
\title{Download NHANES Public-Use Linked Mortality Files}
\usage{
nhanes_mortality_download(
  cycles = NULL,
  refresh = FALSE,
  quiet = !getOption("nhanesR.verbose", TRUE)
)
}
\arguments{
\item{cycles}{Character vector of cycle labels. Defaults to all cycles with
a public-use LMF. See \code{\link[=nhanes_cycles]{nhanes_cycles()}} and \code{\link[=nhanes_lmf_cycles]{nhanes_lmf_cycles()}}.}

\item{refresh}{Logical. Re-download even if a cached file exists? Default
\code{FALSE}.}

\item{quiet}{Logical. Suppress download messages? Default uses the
\code{nhanesR.verbose} option.}
}
\value{
Invisibly, a named character vector of local file paths (one per
cycle). The primary side-effect is writing files to the cache directory
under \verb{mortality/dat/}.
}
\description{
Downloads the fixed-width \code{.dat} mortality files from the CDC FTP server
for one or more NHANES cycles. Files are cached locally; re-downloading
is skipped unless \code{refresh = TRUE}.
}
\details{
The public-use LMF provides mortality follow-up through \strong{December 31, 2019}
for NHANES 1999-2018 and NHANES III. Files were released in April 2022 and
will not be updated (the 2022-linked restricted-use files require RDC access).
}
\examples{
\donttest{
# Download all available cycles
nhanes_mortality_download()

# Download specific cycles
nhanes_mortality_download(c("2013-2014", "2015-2016", "2017-2018"))
}
}
\seealso{
\code{\link[=nhanes_mortality_parse]{nhanes_mortality_parse()}}, \code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}},
\code{\link[=nhanes_survival_prep]{nhanes_survival_prep()}}
}
