% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mortality.R
\name{nhanes_mortality_parse}
\alias{nhanes_mortality_parse}
\title{Parse NHANES Linked Mortality Files into data frames}
\usage{
nhanes_mortality_parse(cycles = NULL, refresh = FALSE, download = TRUE)
}
\arguments{
\item{cycles}{Character vector of cycle labels. Defaults to all available.}

\item{refresh}{Logical. Re-parse even if a cached RDS exists? Default \code{FALSE}.}

\item{download}{Logical. Auto-download missing \code{.dat} files? Default \code{TRUE}.}
}
\value{
A named list of data frames. Each data frame contains:
\describe{
\item{SEQN}{Respondent sequence number (join key to NHANES data).}
\item{ELIGSTAT}{Eligibility: 1=eligible; 2=under 18; 3=insufficient data.}
\item{MORTSTAT}{Vital status: 0=assumed alive; 1=assumed deceased.}
\item{UCOD_LEADING}{Underlying cause of death (11-category ICD-10 recode).}
\item{DIABETES}{Diabetes mentioned on death certificate (1=yes).}
\item{HYPERTEN}{Hypertension mentioned on death certificate (1=yes).}
\item{PERMTH_INT}{Months of follow-up from interview date.}
\item{PERMTH_EXM}{Months of follow-up from examination date.}
}
}
\description{
Reads the fixed-width \code{.dat} files (downloading them first if needed) and
returns a named list of data frames, one per cycle.
}
\details{
Variable labels are attached as the \code{"label"} attribute on each column,
following the \code{haven}/\code{labelled} convention.
}
\note{
For select records, \code{PERMTH_INT}, \code{PERMTH_EXM}, and \code{UCOD_LEADING}
contain \strong{synthetic (perturbed) values} introduced by CDC to reduce
re-identification risk. \code{MORTSTAT} and \code{ELIGSTAT} are not perturbed.
}
\examples{
\donttest{
lmf <- nhanes_mortality_parse(c("2015-2016", "2017-2018"))
lmf[["2015-2016"]]
}
}
\seealso{
\code{\link[=nhanes_mortality_download]{nhanes_mortality_download()}} to download the raw \code{.dat} files;
\code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}} to join parsed mortality data onto an analytic
dataset.
}
