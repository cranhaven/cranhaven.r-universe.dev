% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{nhanes_download_analyte}
\alias{nhanes_download_analyte}
\title{Download NHANES files for an analyte using the CDC variable catalog}
\usage{
nhanes_download_analyte(
  term,
  cycles,
  component = "Laboratory",
  keep_vars = NULL,
  refresh = FALSE,
  add_cycle_col = TRUE
)
}
\arguments{
\item{term}{Character. Search term passed to \code{\link[=nhanes_variable_map]{nhanes_variable_map()}}.}

\item{cycles}{Character. One or more cycle labels (e.g. \code{"1999-2000"}).
See \code{\link[=nhanes_cycles]{nhanes_cycles()}}.}

\item{component}{Character or \code{NULL}. NHANES component to search.
Default \code{"Laboratory"}.}

\item{keep_vars}{Character vector or \code{NULL}. Passed to \code{\link[=nhanes_variable_map]{nhanes_variable_map()}}
to disambiguate serum from urine forms of the same analyte.}

\item{refresh}{Logical. Re-download even if cached? Default \code{FALSE}.}

\item{add_cycle_col}{Logical. Add a \code{cycle} column to each data frame?
Default \code{TRUE}.}
}
\value{
If a single cycle is requested, a data frame. If multiple cycles
are requested, a named list of data frames keyed by cycle label.
}
\description{
A smarter alternative to \code{\link[=nhanes_download]{nhanes_download()}} for analytes whose file name
changed across cycles (e.g. total cholesterol: \code{LAB13} -> \code{L13_B} -> \code{L13_C}
-> \code{TCHOL_D} onward). Uses \code{\link[=nhanes_variable_map]{nhanes_variable_map()}} to look up the correct
CDC file name for each cycle, then downloads using the exact catalog name.
}
\examples{
\donttest{
cycles <- nhanes_cycles()[1:10, "cycle"]

# Total cholesterol -- file name changed in 1999-2004; this handles it
tchol_list <- nhanes_download_analyte("total cholesterol", cycles)

# Serum creatinine (keep_vars excludes urine creatinine)
scr_list <- nhanes_download_analyte("creatinine", cycles,
                                    keep_vars = c("LBXSCR","LBDSCR","LB2SCR"))
}
}
\seealso{
\code{\link[=nhanes_variable_map]{nhanes_variable_map()}} to inspect the per-cycle file/variable
lookup before downloading; \code{\link[=nhanes_harmonize]{nhanes_harmonize()}} to rename and stack the
returned list; \code{\link[=nhanes_download]{nhanes_download()}} for downloading by exact file code.
}
