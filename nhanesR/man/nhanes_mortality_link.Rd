% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mortality.R
\name{nhanes_mortality_link}
\alias{nhanes_mortality_link}
\title{Link mortality data onto an NHANES analytic dataset}
\usage{
nhanes_mortality_link(
  nhanes_data,
  cycles = NULL,
  keep_vars = NULL,
  download = TRUE,
  seqn_col = "SEQN",
  cycle_col = "cycle"
)
}
\arguments{
\item{nhanes_data}{A data frame containing NHANES participants.}

\item{cycles}{Character vector of \code{"YYYY-YYYY"} cycle labels present in
\code{nhanes_data}. Inferred from \code{cycle_col} when omitted.}

\item{keep_vars}{Character vector of LMF variables to retain. Defaults to
all: \code{c("ELIGSTAT", "MORTSTAT", "UCOD_LEADING", "DIABETES", "HYPERTEN", "PERMTH_INT", "PERMTH_EXM")}.}

\item{download}{Logical. Download missing LMF files automatically? Default
\code{TRUE}.}

\item{seqn_col}{Character. Name of the participant sequence-number column
in \code{nhanes_data}. Default \code{"SEQN"} (nhanesR / CDC standard). Use
\code{"seqn"} for \code{nhanesdata} output.}

\item{cycle_col}{Character. Name of the cycle column in \code{nhanes_data}.
Default \code{"cycle"} (\code{"YYYY-YYYY"} labels). Use \code{"year"} for \code{nhanesdata}
output, where the column contains integer start years (e.g. 1999, 2001).}
}
\value{
\code{nhanes_data} with LMF columns appended. Rows with no mortality
record (SEQNs absent from the LMF) will have \code{NA} for all LMF columns;
this should not occur for continuous NHANES 1999-2018.
}
\description{
Performs a left join of the parsed LMF onto a data frame containing NHANES
participants, matched on the participant sequence number. Automatically
handles multiple cycles by row-binding the appropriate LMF files before
joining.
}
\details{
Data from any source -- \code{\link[=nhanes_download]{nhanes_download()}}, \code{nhanesA}, or \code{nhanesdata} --
can be linked by supplying the appropriate column name arguments. For
example, \code{nhanesdata} stores the sequence number as \code{seqn} (integer) and
the cycle as \code{year} (integer start year, e.g. 1999); pass
\verb{seqn_col = "seqn", cycle_col = "year"} and both are handled automatically.
}
\examples{
\donttest{
demo <- nhanes_download("DEMO", "2015-2016")
demo_mort <- nhanes_mortality_link(demo)
}
}
\seealso{
\code{\link[=nhanes_survival_prep]{nhanes_survival_prep()}} to convert the linked data into a survival
dataset; \code{\link[=nhanes_lmf_cycles]{nhanes_lmf_cycles()}} for cycles with a public-use LMF;
\code{\link[=nhanes_stack]{nhanes_stack()}} to row-bind multi-cycle data before linking.
}
