% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{nhanes_download}
\alias{nhanes_download}
\title{Download and cache NHANES XPT data files}
\usage{
nhanes_download(file_code, cycles, refresh = FALSE, add_cycle_col = TRUE)
}
\arguments{
\item{file_code}{Character. The NHANES file code(s), without suffix or
extension (e.g. \code{"DEMO"}, \code{"BPX"}, \code{"TRIGLY"}). Case-insensitive.
Can be a vector to download multiple files.}

\item{cycles}{Character. One or more cycle labels (e.g. \code{"2015-2016"}).
See \code{\link[=nhanes_cycles]{nhanes_cycles()}}. If multiple cycles are given, the same file code
is downloaded for each.}

\item{refresh}{Logical. Re-download and re-parse even if cached? Default
\code{FALSE}.}

\item{add_cycle_col}{Logical. Add a \code{cycle} column to each returned data
frame? Default \code{TRUE}. Required for \code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}}.}
}
\value{
If a single file_code and single cycle are requested, a data frame.
If multiple file_codes or cycles are requested, a named list of data frames
with names of the form \code{"{file_code}_{cycle}"}.
}
\description{
Downloads one or more NHANES component files in SAS transport (XPT) format
from the CDC website, parses them into R data frames, attaches variable
labels, and caches the results locally as RDS files.
}
\details{
\subsection{Finding valid file codes}{

File codes are the base names CDC assigns to each data file, without the
cycle-letter suffix or \code{.xpt} extension. For example, the Demographics file
is always \code{"DEMO"}, and the blood pressure examination file is \code{"BPX"}.

Use \code{\link[=nhanes_manifest]{nhanes_manifest()}} to browse all files available for a given cycle and
component. The \code{file_name} column of the manifest shows the full CDC name
including the cycle suffix (e.g. \code{"TCHOL_I"}); strip the trailing
underscore-letter to get the base code for \code{nhanes_download()}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{m <- nhanes_manifest("2015-2016", "Laboratory")
m[, c("file_name", "description")]

# Base codes ready for nhanes_download():
sub("_[A-Z]$", "", m$file_name)
}\if{html}{\out{</div>}}

Note that some analyte file names changed across cycles (e.g. total
cholesterol: \code{LAB13} -> \code{L13_B} -> \code{TCHOL_D} onward). For those cases,
use \code{\link[=nhanes_download_analyte]{nhanes_download_analyte()}} instead, which looks up the correct CDC
filename for each cycle automatically via the variable catalog.
}

\subsection{Invalid file codes}{

File codes are not validated before the download attempt. If an unknown
code is supplied, CDC returns HTTP 200 with an HTML error page rather than
a 404. nhanesR detects this via the \code{Content-Type} header and aborts with
a message directing you to \code{\link[=nhanes_manifest]{nhanes_manifest()}} to confirm the correct name.
}

\subsection{Variable label encoding}{

NHANES XPT files were produced by SAS, which writes variable labels in the
system locale of the generating server — typically Latin-1 (ISO 8859-1).
Some biochemistry files (notably \code{BIOPRO} and its predecessors \code{L40_C}
onward) use the Latin-1 byte \code{0xB5} for the micro prefix in SI unit
strings such as \code{umol/L}. That byte is not valid UTF-8, so any downstream
code that runs regular expressions over label attributes will receive an
\code{"unable to translate ... to a wide string"} warning and the label will be
skipped.

nhanesR guards against this internally by passing labels through
\code{iconv(..., to = "UTF-8", sub = "")} before pattern matching in
\code{\link[=nhanes_harmonize]{nhanes_harmonize()}}. If you read label attributes directly in your own
code, apply the same conversion:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{labels <- iconv(
  vapply(df, function(col) attr(col, "label") \%||\% "", character(1L)),
  to = "UTF-8", sub = ""
)
}\if{html}{\out{</div>}}
}
}
\examples{
\donttest{
# Browse available Laboratory files for a cycle, then download by base code
m <- nhanes_manifest("2015-2016", "Laboratory")
m[, c("file_name", "description")]   # see what's available
bpx <- nhanes_download("BPX", "2015-2016")

# Single file, single cycle
demo <- nhanes_download("DEMO", "2015-2016")

# Multiple cycles (returns list)
demos <- nhanes_download("DEMO", c("2013-2014", "2015-2016", "2017-2018"))

# Multiple files, single cycle
files <- nhanes_download(c("DEMO", "BPX", "TRIGLY"), "2015-2016")
}
}
\seealso{
\code{\link[=nhanes_manifest]{nhanes_manifest()}} to browse available file codes;
\code{\link[=nhanes_download_analyte]{nhanes_download_analyte()}} for analytes whose file name changed across
cycles; \code{\link[=nhanes_cycles]{nhanes_cycles()}} for valid cycle labels.
}
