% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mortality.R
\name{nhanes_survival_prep}
\alias{nhanes_survival_prep}
\title{Prepare an NHANES-LMF dataset for survival analysis}
\usage{
nhanes_survival_prep(
  data,
  origin = c("exam", "interview"),
  time_unit = c("months", "years"),
  cause = NULL,
  weight_var = NULL,
  seqn_col = "SEQN",
  cycle_col = "cycle"
)
}
\arguments{
\item{data}{A data frame from \code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}}.}

\item{origin}{Character. Follow-up origin: \code{"interview"} (uses \code{PERMTH_INT})
or \code{"exam"} (uses \code{PERMTH_EXM}). For analyses involving lab or examination
data, use \code{"exam"}.}

\item{time_unit}{Character. \code{"months"} (default, as stored in LMF) or
\code{"years"} (divides by 12).}

\item{cause}{Character or \code{NULL}. If supplied, creates a cause-specific event
indicator \code{event_cause} that is 1 only when \code{UCOD_LEADING} matches this
code and \code{MORTSTAT == 1}. See \code{\link[=nhanes_ucod_labels]{nhanes_ucod_labels()}} for valid codes.
For all-cause mortality, leave \code{NULL}.}

\item{weight_var}{Character. Name of the survey weight column to carry
through. The column is renamed to \code{survey_weight} in the output.
If \code{NULL}, no weight is attached.}

\item{seqn_col}{Character. Name of the participant sequence-number column.
Default \code{"SEQN"}. Use \code{"seqn"} for \code{nhanesdata} output.}

\item{cycle_col}{Character. Name of the cycle column. Default \code{"cycle"}.
Use \code{"year"} for \code{nhanesdata} output.}
}
\value{
A data frame with additional columns:
\describe{
\item{time}{Follow-up time in specified units.}
\item{event}{All-cause mortality indicator (0/1).}
\item{event_cause}{Cause-specific indicator, if \code{cause} supplied.}
\item{survey_weight}{Survey weight, renamed from \code{weight_var}.}
\item{.eligstat_dropped}{Attribute recording how many rows were removed.}
}
}
\description{
Takes a linked NHANES-mortality data frame (from \code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}})
and returns a dataset ready for use with \code{\link[survival:Surv]{survival::Surv()}}, with:
}
\details{
\itemize{
\item Ineligible participants (\code{ELIGSTAT != 1}) removed with a warning
\item A \code{time} column (in months or years) from the specified follow-up origin
\item An \code{event} column (0/1) from \code{MORTSTAT}
\item Optional cause-specific event indicator based on \code{UCOD_LEADING}
\item Survey weight column renamed and validated
}
}
\section{Eligibility filtering}{

Participants with \code{ELIGSTAT != 1} are \strong{automatically removed} with a
warning. This includes those under 18 at time of survey (\code{ELIGSTAT == 2})
and those with insufficient identifying data for linkage (\code{ELIGSTAT == 3}).
The number dropped is reported and attached as an attribute.
}

\section{Asymmetric follow-up across cycles}{

All public-use LMF files censor at December 31, 2019. Participants
enrolled in later cycles (e.g. 2017-2018) have substantially shorter
maximum follow-up than those enrolled in 1999-2000. This function warns
when multiple cycles are detected, as this asymmetry must be accounted
for in any pooled analysis.
}

\section{Survey weights}{

NHANES provides three families of survey weight, each correcting for a
different sampling stage. Choosing the wrong weight produces biased
point estimates and incorrect standard errors.\tabular{ll}{
   Weight \tab When to use \cr
   \code{WTINT2YR} \tab Interview-only data (questionnaires, no lab/exam) \cr
   \code{WTMEC2YR} \tab Any examination or laboratory component \cr
   \code{WTSAF2YR} \tab Analytes from the \strong{fasting subsample} (triglycerides, glucose, insulin, calculated LDL) \cr
}


The fasting subsample weight (\code{WTSAF2YR}) is a \emph{statistical} probability
weight -- not a body-weight measurement -- that accounts for the additional
random subsampling of participants asked to fast before their blood draw.
Fasting participants are a minority of all MEC attendees; using \code{WTMEC2YR}
for fasting analytes ignores this extra subsampling step and will give
incorrect population estimates.

For pooled multi-cycle analyses divide the 2-year weight by the number of
cycles pooled, or use the pre-computed 4-year weight \code{WTMEC4YR} where
available. See the NHANES analytic guidelines for details.
}

\section{Perturbed variables}{

\code{PERMTH_INT}, \code{PERMTH_EXM}, and \code{UCOD_LEADING} contain synthetic values
for select records (CDC data perturbation to reduce re-identification risk).
\code{MORTSTAT} is not perturbed. Cause-specific analyses using \code{UCOD_LEADING}
should be interpreted with this in mind.
}

\examples{
\donttest{
demo_list <- nhanes_download("DEMO", c("2013-2014", "2015-2016"))
demo <- nhanes_stack(demo_list)
demo_mort <- nhanes_mortality_link(demo)

# All-cause mortality, exam origin, MEC 2-year weight
surv_data <- nhanes_survival_prep(
  demo_mort,
  origin     = "exam",
  time_unit  = "years",
  weight_var = "WTMEC2YR"
)

# Cause-specific: cardiovascular (code "001")
surv_data_cvd <- nhanes_survival_prep(
  demo_mort,
  origin = "exam",
  cause  = "001",
  weight_var = "WTMEC2YR"
)

# Use with survival package
library(survival)
library(survey)
design <- svydesign(
  id      = ~SDMVPSU,
  strata  = ~SDMVSTRA,
  weights = ~survey_weight,
  nest    = TRUE,
  data    = surv_data
)
}
}
\seealso{
\code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}} which produces the input for this
function; \code{\link[=nhanes_followup_summary]{nhanes_followup_summary()}} to check follow-up time by cycle;
\code{\link[=nhanes_ucod_labels]{nhanes_ucod_labels()}} for cause-of-death codes accepted by \code{cause}.
}
