% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{nhanes_stack}
\alias{nhanes_stack}
\title{Stack NHANES data across multiple cycles}
\usage{
nhanes_stack(..., fill = TRUE)
}
\arguments{
\item{...}{Named or unnamed data frames, each representing one cycle's
data for the same component.}

\item{fill}{Logical. If \code{TRUE} (default), columns present in some but not
all cycles are filled with \code{NA} in cycles where they are absent. If
\code{FALSE}, only columns common to all cycles are retained.}
}
\value{
A single data frame with all cycles stacked. A \code{cycle} column is
always included.
}
\description{
Row-binds the same component across multiple cycles, enforcing that the
\code{cycle} column is present and handling variable name changes across cycles.
}
\examples{
\donttest{
demos <- nhanes_download("DEMO", c("2013-2014", "2015-2016", "2017-2018"))
stacked <- nhanes_stack(demos)
}
}
\seealso{
\code{\link[=nhanes_harmonize]{nhanes_harmonize()}} which calls this internally and also renames
variables across cycles; \code{\link[=nhanes_merge]{nhanes_merge()}} to join components by SEQN;
\code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}} which expects a stacked data frame as input.
}
