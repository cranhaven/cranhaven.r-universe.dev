destination: rENA/
navbar:
  title: "rENA"
  type: inverse
  left:
    - text: "Home"
      href: index.html
    - text: "Reference"
      href: "reference/"
    - text: "Changelog"
      href: "news/"
    - text: "Coverage"
      href: "coverage/"
  right:
    - icon: fa-gitlab
      href: https://gitlab.com/epistemic-analytics/qe-packages/rena/
reference:
  - title: "Creating Models"
    desc: >
      Functions for the generation of ENA models, all of which will return, by default, an
      S3 object with the class of `ena.set` that can be supplied to the other
      functions.
    contents:
      - ena
      - starts_with("ena.accum")
      - ena.make.set
  - title: "Plotting Models"
    desc: Functions to plot an `ena.set`, all of which return an `ena.plot` object that
      can be supplied to the other functions.
    contents:
      - plot.ena.set
      - matches("add_")
      - matches("ena.plot")

  - title: internal
    contents:
    - ENARotationSet
    - ENAdata
    - ENAset
    - ENAplot
    - RS.data
    - as.ena.co.occurrence
    - as.ena.matrix
    - as.ena.metadata
    - as.matrix.ena.connections
    - as.matrix.ena.line.weights
    - as.matrix.ena.matrix
    - as.matrix.ena.nodes
    - as.matrix.ena.points
    - as.matrix.ena.rotation.matrix
    - as.matrix.row.connections
    - as_trajectory
    - $.ena.matrix
    - $.ena.metadata
    - $.ena.points
    - $.line.weights
    - clear
    - combn_c2
    - connection.matrix
    - ena.conversations
    - ena.correlations
    - ena.group
    - ena.rotate.by.mean
    - ena.rotation.h
    - ena.set.creator
    - ena.svd
    - ena.writeup
    - ena_correlation
    - find_code_cols
    - find_dimension_cols
    - find_meta_cols
    - fun_cohens.d
    - fun_skip_sphere_norm
    - fun_sphere_norm
    - means_rotate
    - merge_columns_c
    - methods_report
    - methods_report_stream
    - move_nodes_to_unit_circle
    - move_nodes_to_unit_circle_with_equal_space
    - namesToAdjacencyKey
    - prepare_trajectory_data
    - print.ena.set
    - project_in
    - rENA
    - remove_meta_data
    - scale.ena.set
    - show
    - vector_to_ut
    - with_means
    - with_trajectory
