% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{pra_app}
\alias{pra_app}
\title{Launch the PRA Risk Analysis Agent App}
\usage{
pra_app(
  model = "llama3.2",
  rag = TRUE,
  embed_model = "nomic-embed-text",
  port = NULL,
  launch.browser = TRUE
)
}
\arguments{
\item{model}{Character. Ollama model name (default \code{"llama3.2"}).}

\item{rag}{Logical. Whether to enable RAG context retrieval (default \code{TRUE}).}

\item{embed_model}{Character. Ollama embedding model for RAG (default
\code{"nomic-embed-text"}).}

\item{port}{Integer. Port for the Shiny app (default \code{NULL} lets Shiny choose).}

\item{launch.browser}{Logical. Whether to open a browser (default \code{TRUE}).}
}
\value{
None. This function is called to launch the shiny app.
}
\description{
Starts a Shiny app that provides an interactive chat interface to the PRA
risk analysis agent. The agent can select and execute PRA tools (Monte Carlo
simulation, EVM, Bayesian inference, etc.) in response to natural language
questions. Uses shinychat for a polished streaming chat experience with
inline tool result display.
}
\details{
Requires Ollama to be running locally with the specified model downloaded.
}
\examples{
\dontrun{
# Ensure Ollama is running, then:
pra_app()

# With a specific model:
pra_app(model = "qwen2.5")
}

}
