% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smm.R
\name{print.smm}
\alias{print.smm}
\title{Print method for SMM results.}
\usage{
\method{print}{smm}(x, ...)
}
\arguments{
\item{x}{An object of class "smm" containing the SMM results.}

\item{...}{Additional arguments (not used).}
}
\value{
None. The function prints the SMM results to the console.
}
\description{
This function defines how to print the results of the Second Moment Method
(SMM) analysis. It formats the output to display the total mean, variance,
and standard deviation in a readable manner.
}
\examples{
mean <- c(10, 15, 20)
var <- c(4, 9, 16)
cor_mat <- matrix(c(
  1, 0.5, 0.3,
  0.5, 1, 0.4,
  0.3, 0.4, 1
), nrow = 3, byrow = TRUE)
result <- smm(mean, var, cor_mat)
print(result)

# Without correlation matrix (independent tasks)
result <- smm(mean, var)
print(result)

}
