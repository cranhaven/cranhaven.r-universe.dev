% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools.R
\name{execute_command}
\alias{execute_command}
\title{Parse and execute a slash command}
\usage{
execute_command(input)

execute_command(input)
}
\arguments{
\item{input}{Character string of user input (e.g. "/mcs n=5000 tasks=\link{...}").}
}
\value{
A list with \code{ok} (logical) and \code{result} (character or ContentToolResult).

A list with \code{ok} and \code{result}.
}
\description{
Parses user input starting with \code{/}, validates arguments, and executes
the corresponding tool function. Returns a list with \code{ok} (logical) and
\code{result} (character string — either the tool output or a help/error message).
}
\keyword{internal}
