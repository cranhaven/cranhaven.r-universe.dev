% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{phone_parse}
\alias{phone_parse}
\title{Parse a Phone Number}
\usage{
phone_parse(number, default_region = NULL)
}
\arguments{
\item{number}{Character vector of phone numbers.}

\item{default_region}{ISO 3166-1 alpha-2 region code (e.g., \code{"NZ"}, \code{"US"})
used when numbers are in national format (no leading \code{+}). If \code{NULL},
numbers without a \code{+} prefix will fail to parse.}
}
\value{
A list of parsed phone number lists, each with elements:
\describe{
\item{\code{raw}}{The original input string.}
\item{\code{country_code}}{Country calling code (e.g., \code{"64"} for NZ).}
\item{\code{national_number}}{The national significant number (digits only).}
\item{\code{region}}{ISO 3166-1 alpha-2 region code.}
\item{\code{valid}}{Logical indicating if the number is valid.}
}
Returns a list with \code{valid = FALSE} for numbers that cannot be parsed.
}
\description{
Parses a phone number string into its components: country code, national
number, and region. Numbers can be provided in international format (with
leading \code{+}) or national format (with \code{default_region}).
}
\examples{
phone_parse("+64211234567")
phone_parse("021 123 4567", default_region = "NZ")
phone_parse(c("+12125551234", "+442071234567"))

}
