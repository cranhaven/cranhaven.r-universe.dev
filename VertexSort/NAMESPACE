export(vertex.sort, export.to.cytoscape, dpr, sdpr, idpr, odpr, dnpr)

S3method(print, vertex.sort)
S3method(summary, vertex.sort)
S3method(print, summary.vertex.sort)

import(snowfall, igraph)

