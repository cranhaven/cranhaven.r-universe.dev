% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assignments.R
\name{assign_treatments}
\alias{assign_treatments}
\alias{build_ra_args}
\alias{compute_assignments}
\alias{assign_treatments.data.frame}
\alias{assign_treatments.data.table}
\title{Adaptively Assign Treatments in a Period}
\usage{
assign_treatments(
  current_data,
  probs,
  blocking = NULL,
  clustering = NULL,
  conditions,
  condition_col = NULL,
  cluster_col = NULL,
  sim_type
)

build_ra_args(
  current_data,
  probs = NULL,
  conditions,
  blocking,
  clustering,
  cluster_col = NULL,
  dt
)

compute_assignments(
  current_data,
  probs,
  conditions,
  blocking,
  clustering,
  cluster_col = NULL
)

\method{assign_treatments}{data.frame}(
  current_data,
  probs,
  blocking,
  clustering,
  conditions,
  condition_col = NULL,
  cluster_col = NULL,
  sim_type
)

\method{assign_treatments}{data.table}(
  current_data,
  probs,
  blocking,
  clustering,
  conditions,
  condition_col = NULL,
  cluster_col = NULL,
  sim_type
)
}
\arguments{
\item{current_data}{A \code{data.frame} or \code{data.table} with only observations from the current sampling period.}

\item{probs}{Named numeric vector; probability of assignment for each treatment condition.}

\item{blocking}{Logical; Whether or not treatment blocking is occurring}

\item{clustering}{Logical; Whether or not treatment clustering is occurring}

\item{conditions}{Character vector of treatment condition labels. If a control group is specified
the \code{names} attribute should be present with the control group labeled \code{"control"}.}

\item{condition_col}{Column name of \code{current_data} which holds original treatment assignments.}

\item{cluster_col}{Column name of \code{current_data} which holds cluster assignments.}

\item{sim_type}{String; Type of simulation to conduct, either \code{"resim"}, \code{"param"}, or \code{"test"},
for a resimulated rct, simulation from population parameters, or simulation for the randomization joint test.}

\item{dt}{Logical. Whether \code{current_data} is a data.table.}
}
\value{
Updated \code{tibble} or \code{data.table} with the new treatment conditions for each observation, and whether imputation is required.
If this treatment is different from the original experiment, \code{impute_req = 1}, otherwise it is 0.

A list with \code{fn} (the randomizr function) and \code{args} (its arguments).

Character vector of length \code{nrow(current_data)} with treatment assignments.
}
\description{
Assigns new treatments for an assignment wave based on the assignment probabilities provided from
\code{\link[=compute_bandit]{compute_bandit()}}. Assignments are made randomly with the given probabilities using \code{\link[randomizr:block_ra]{randomizr::block_ra()}},
\code{\link[randomizr:complete_ra]{randomizr::complete_ra()}}, \code{\link[randomizr:cluster_ra]{randomizr::cluster_ra()}}, or \code{\link[randomizr:block_and_cluster_ra]{randomizr::block_and_cluster_ra()}}
depending on whether blocking and/or clustering are used.
}
\section{Functions}{
\itemize{
\item \code{build_ra_args()}: Selects the appropriate \code{{randomizr}} function and constructs its argument list
based on whether blocking and/or clustering are requested.

\item \code{compute_assignments()}: Pre-allocates a character vector and fills treatment assignments
for bandit and randomly assigned subsets separately, using the appropriate
randomizr function built by \code{\link[=build_ra_args]{build_ra_args()}}.

}}
\seealso{
\itemize{
\item \code{\link[randomizr:block_ra]{randomizr::block_ra()}}
\item \code{\link[randomizr:complete_ra]{randomizr::complete_ra()}}
\item \code{\link[randomizr:cluster_ra]{randomizr::cluster_ra()}}
\item \code{\link[randomizr:block_and_cluster_ra]{randomizr::block_and_cluster_ra()}}
}

Other assign:
\code{\link[=compute_bandit]{compute_bandit()}},
\code{\link[=compute_prior]{compute_prior()}}
}
\concept{assign}
\keyword{internal}
