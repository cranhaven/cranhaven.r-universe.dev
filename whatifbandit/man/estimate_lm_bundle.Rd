% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contrast.R
\name{estimate_lm_bundle}
\alias{estimate_lm_bundle}
\title{Fit a Robust Linear Probability Model and Compute Contrasts}
\usage{
estimate_lm_bundle(
  ipw,
  estimator,
  sim_results,
  col_names,
  clustering,
  conditions,
  num_clusters,
  dt,
  contrasts_list
)
}
\arguments{
\item{ipw}{Logical. If \code{TRUE} IPW-weighted LPM; if
\code{FALSE}, fits the unweighted OLS LPM.}

\item{estimator}{Character string labeling the estimator, either
\code{"IPW"} or \code{"OLS"}}

\item{col_names}{List holding the columns required from the provided data as strings and symbols.}

\item{clustering}{Logical; Whether or not treatment clustering is occurring}

\item{conditions}{Character vector of treatment condition labels. If a control group is specified
the \code{names} attribute should be present with the control group labeled \code{"control"}.}

\item{dt}{Logical. If \code{TRUE} returns a \code{\link[data.table:data.table]{data.table::data.table()}}; otherwise returns a \code{\link[tibble:tibble]{tibble::tibble()}}. Default \code{FALSE}.}

\item{contrasts_list}{List of 1 x k row vector contrasts to compute.}
}
\value{
A named list with three elements: \code{means}, \code{contrasts}, \code{model}. \code{contrasts} are the
computed contrasts, the other 2 elements are the output of \code{\link[=estimate_lm]{estimate_lm()}}
}
\description{
Internal helper that wraps linear model estimation for both IPW
and OLS estimators, returning arm-level mean estimates, precomputed
contrasts, and optionally the fitted model object.
}
\keyword{internal}
