% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_rct.R
\name{check_rct_args}
\alias{check_rct_args}
\alias{check_cols}
\alias{check_data}
\alias{check_period_method}
\title{Validates Inputs For \code{\link[=mab_from_rct]{mab_from_rct()}}}
\usage{
check_rct_args(
  data,
  control_augment,
  random_assign_prop,
  period_method,
  time_unit,
  period_length,
  prior_periods,
  discount_rate,
  delayed_feedback,
  whole_experiment,
  col_names,
  verbose,
  ndraws,
  r,
  keep_data,
  keep_models,
  blocking
)

check_cols(
  period_method,
  time_unit,
  delayed_feedback,
  col_names,
  data,
  verbose,
  blocking
)

check_data(
  data,
  col_names,
  period_method,
  period_length,
  time_unit,
  delayed_feedback
)

check_period_method(period_method, time_unit, verbose, period_length)
}
\arguments{
\item{data}{A \code{data.frame}, \code{data.table}, or any object which inherits from \code{data.frame}, containing input data from the trial. This should be the results
of a traditional Randomized Controlled Trial (RCT).}

\item{control_augment}{Minimum proportion of each treatment assignment wave guaranteed to receive the treatment labeled as \code{"Control"}. Ranges from 0 to 1,
and the default is 0. Adjustment is always made after the adjustment from random_assign_prop.}

\item{random_assign_prop}{Proportion of each treatment wave assigned via static, equal
probabilities of assignment. Adaptive probabilities are updated by \code{p * (1 - random_assign_prop) + random_assign_prop * 1/k}, where k is the number of treatment arms.}

\item{period_method}{A character string; one of \code{"date"}, \code{"batch"}, or \code{"individual"}, to define
the assignment into treatment waves. When using \code{"batch"} or \code{"individual"}, ensure your dataset
is pre-arranged in the proper order observations should be considered so that groups are assigned
correctly. For "date", observations will be considered in chronological order. \code{"individual"}
assignment can be computationally intensive for larger datasets.}

\item{time_unit}{A character string specifying the unit of time for assigning periods when
\code{period_method ="date"}. Acceptable values are \verb{"day", "week",} or \code{"month"}. \code{"month"} does not
require an additional column with the months of each observation, but it can accept a separate
\code{month_col}. If \code{month_col} is specified, the periods follow the calendar months strictly, and
when it is not specified months are simply used as the time interval. For example if a dataset
has dates starting on July 26th, under month based assignment and a specified \code{month_col} the
dates July 26th and August 3rd would be in different periods, but if the \code{month_col} was not
specified, they would be in the same period because the dates are less than one month apart.}

\item{period_length}{A positive integer; represents the length of each treatment period. If
\code{period_method} is "date", this length refers the number of units specified in \code{time_unit}.
(i.e., if \code{"day"}, 10 would be 10 days). If \code{period_method} = \code{"batch"}, this refers to the
number of units in each batch.}

\item{prior_periods}{A positive integer; number of previous periods to use in the treatment
assignment model. Default is \code{NULL}, where all prior periods are considered. See below for
details.}

\item{discount_rate}{Rate for discounting observations from earlier periods when updating
assignment probabilities. A value between 0 and 1, where outcomes from \code{k} periods ago are
weighted by \code{discount_rate^k}. Default is 1 for no discounting.}

\item{delayed_feedback}{Logical; if \code{FALSE}, assumes instantaneous feedback for outcomes, as soon
as a treatment is assigned, the outcome is realized and known. If \code{TRUE}, delayed feedback is
assumed, so as soon as treatment is assigned, a potential outcome is realized, but it is not
known to the simulation, until a certain date. When re-computing the adaptive assignment
probabilities, outcomes that have not been observed on the date of
assignment are treated as failures.}

\item{whole_experiment}{Logical; if \code{TRUE}, uses all past experimental data for imputing
outcomes. If \code{FALSE}, uses only data available up to the current period. In large datasets or
with a high number of periods, setting this to \code{FALSE} can be more computationally intensive,
though not a significant contributor to total run time. Default is \code{FALSE}.}

\item{col_names}{List holding the columns required from the provided data as strings and symbols.}

\item{verbose}{Logical; whether or not to print intermediate messages. Default is \code{FALSE}.}

\item{ndraws}{Number of draws used to approximate Thompson sampling probabilities. Used only when
direct calculation fails or overflows. Default is 5000 but can be raised or lowered depending on
performance and accuracy concerns.}

\item{r}{Positive integer; number of replications (under different random seed). Replications of
the MAB procedure on a fixed dataset provides important diagnostic information on the
stochasticity/variance of the re-simulation method. Replications can be conducted in parallel, by
setting an appropriate \code{\link[future:plan]{future::plan()}}. See details below.}

\item{keep_data}{Logical; Whether or not to keep the final data from each trial. Recommended
\code{FALSE}. When\code{ r = 1} the final data is always kept and reported.}

\item{keep_models}{Logical; Whether or not to keep the final IPW and OLS models from each trial. Recommended
\code{FALSE}. When\code{ r = 1} models are always kept and reported. Required to be \code{TRUE} to compute
arbitrary pairwise contrasts. Only utilized under clustering.}

\item{blocking}{Logical; Whether or not treatment blocking is occurring}
}
\value{
Throws an error if an argument is missing or misspecified.

Throws an error if columns which are required have not been declared
or are not present in the data, or are the wrong primitive data type. Additionally throws warning messages,
if unnecessary columns have been provided, only when \code{verbose = TRUE}.

Nothing; Throws an error if the provided dataset does not meet the specifications
of the trial based on user settings.

Throws an error if the user is missing necessary arguments to
assign treatments or passes invalid ones.
}
\description{
This function checks to ensure that all required arguments
have been properly passed to \code{\link[=mab_from_rct]{mab_from_rct()}} before continuing with the simulation. When
errors are thrown, user-friendly messages are provided to indicate which argument
was misspecified. Additionally, when \code{verbose = TRUE}, additional warning
messages may be shown if unnecessary arguments are passed.
}
\section{Functions}{
\itemize{
\item \code{check_cols()}: Helper to \code{\link[=check_rct_args]{check_rct_args()}}. This function accepts the user's
settings for the Multi-Arm-Bandit trial, and checks whether columns in the data have been properly
specified based on these settings.

\item \code{check_data()}: Throws an error if the provided dataset does not meet the specifications
of the trial based on user settings.

\item \code{check_period_method()}: Helper to \code{\link[=check_rct_args]{check_rct_args()}}. This function accepts arguments relating
to how treatment waves are assigned, and checks if they are valid, and if all
supporting arguments are passed as necessary.

}}
\seealso{
Other checks:
\code{\link{check_helpers}}
}
\concept{checks}
\keyword{internal}
