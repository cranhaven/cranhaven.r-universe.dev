% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contrast.R
\name{lin_contrast}
\alias{lin_contrast}
\alias{build_contrast_matrices}
\alias{make_contrasts}
\alias{compute_contrast}
\title{Computing Linear Contrasts}
\usage{
build_contrast_matrices(conditions, contrasts = NULL, bandits)

make_contrasts(conditions, ref_idx, type)

compute_contrast(
  C = NULL,
  coefs = NULL,
  vcov = NULL,
  df = NULL,
  model = NULL,
  data = NULL,
  dt,
  conditions = NULL,
  estimator
)
}
\arguments{
\item{conditions}{Vector of treatment conditions}

\item{contrasts}{Character string specifying which pairwise contrasts to
precompute after each replication. One of \code{"control"} (each arm vs. control arm),
\code{"best"} (each arm vs. the MAB-selected best arm),
\code{"both"}, or \code{"all"} (all \code{choose(k, 2)} pairwise comparisons, expensive
for large \code{k}). All contrasts are tested under the two-sided null of no difference.
Defaults to \code{NULL}, arbitrary contrasts can be computed after if \code{keep_models == TRUE}}

\item{bandits}{data.frame or data.table of bandit statistics}

\item{ref_idx}{Index of the reference arm}

\item{type}{Type of contrasts, "best" or "control"}

\item{C}{List of 1 x k contrast vectors}

\item{coefs}{named vector of coefficients}

\item{vcov}{A \verb{k x k} variance-covariance matrix}

\item{df}{Numeric scalar giving the degrees of freedom for the t-test. AW-AIPW only uses t-test in
clustered case.}

\item{model}{An \code{lm_robust} model object}

\item{data}{\code{tibble} or \code{data.table} holding necessary information to complete the simulation.
Also used to store simulation outputs.}

\item{dt}{Whether to compile results into a data.table or data.frame}

\item{estimator}{Character string labeling the estimator, either
\code{"IPW"} or \code{"OLS"}}
}
\value{
A list of 1 row contrast vectors, encoding a single pairwise comparison.

A matrix row vectors containing each contrast to test.

data.table or data.frame of linear contrasts with columns for each treatment arm, the
estimated contrast, standard error, and degrees of freedom.
}
\description{
A set of helper functions for computing linear contrasts of AW-AIPW, IPW, and OLS estimates
}
\section{Functions}{
\itemize{
\item \code{build_contrast_matrices()}: Constructs a matrix of contrast vectors for testing pairwise comparisons
between treatment arms.

\item \code{make_contrasts()}: Creates contrast vectors for linear hypothesis test

\item \code{compute_contrast()}: Compute linear contrasts given a list of contrast vectors
and model inputs. Dispatches to the appropriate method based on the
estimator type and whether clustering is present.

}}
\seealso{
Other estimation:
\code{\link[=compute_iaipw]{compute_iaipw()}},
\code{\link[=estimate_aw_aipw]{estimate_aw_aipw()}},
\code{\link[=estimate_lm]{estimate_lm()}},
\code{\link{inference_helpers}}
}
\concept{estimation}
\keyword{internal}
