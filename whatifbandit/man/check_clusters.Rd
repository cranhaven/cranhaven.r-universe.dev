% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_helpers.R
\name{check_clusters}
\alias{check_clusters}
\alias{check_clusters.data.frame}
\alias{check_clusters.data.table}
\alias{cluster_throw}
\title{Checking Clusters Do Not Persist Across Periods}
\usage{
check_clusters(data, cluster_col)

\method{check_clusters}{data.frame}(data, cluster_col)

\method{check_clusters}{data.table}(data, cluster_col)

cluster_throw(check_clusters, cluster_col)
}
\arguments{
\item{data}{A \code{data.frame}, \code{data.table}, or any object which inherits from \code{data.frame}, containing input data from the trial. This should be the results
of a traditional Randomized Controlled Trial (RCT).}

\item{cluster_col}{String; name of column with cluster indices.}

\item{check_clusters}{Object created in \code{\link[=check_clusters]{check_clusters()}} with all of the clusters
which persist across periods.}
}
\value{
Nothing; Throws a warning if any clusters persist across multiple periods.

Nothing; Throws a warning if clusters persist across periods.
}
\description{
Checks to ensure that each cluster only exists within a single simulation period, because
if this is the case a true clustered design is no longer specified. See details.
}
\details{
The assignment algorithm in \code{\link[=mab_loop]{mab_loop()}} assumes that clusters do not persist across periods. For a
true clustered design, if a cluster persisted across periods, all observations within
it would have to be assigned to the same treatment as in the previous period. In an adaptive
experiment this results in no adaptation, thus this is not implemented
into the algorithm. Instead, the assumption is verified here.
}
\section{Functions}{
\itemize{
\item \code{cluster_throw()}: Internal Helper which handles all data wrangling agnostic
portions of \code{\link[=check_clusters]{check_clusters()}}

}}
\keyword{internal}
