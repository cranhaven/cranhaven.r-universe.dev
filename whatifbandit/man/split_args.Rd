% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_mab.R
\name{split_args}
\alias{split_args}
\title{Split Function Arguments}
\usage{
split_args(time_model = NULL, ...)
}
\arguments{
\item{time_model}{An optional function with signature:

\verb{function(n, conditions, successes, current_period, blocks = NULL, clusters = NULL, ...)}

It returns a vector of \link[lubridate:period]{lubridate::period} objects which will then be added to \code{assignment_dates} to produce \code{success_date}. Used to simulate delayed feedback mechanism
during the trial, so outcomes are imperfectly observed. Only used when\code{assignment_dates} is also supplied. Dates can be generated even when \code{delayed_feedback == FALSE},
but they will not be used. Default \code{NULL}. Other optional arguments Cannot share names with arguments in \code{\link[furrr:furrr_options]{furrr::furrr_options()}}.}

\item{...}{Additional named arguments forwarded to \code{time_model} and \code{\link[furrr:furrr_options]{furrr::furrr_options()}}.}
}
\value{
A named list with 2 elements, \code{furr_args} and \code{time_model_args} each a list of the respective arguments to
\code{\link[furrr:furrr_options]{furrr::furrr_options()}} and the user specified \code{time_model}
}
\description{
Uses \code{\link[methods:formalArgs]{methods::formalArgs()}} to match arguments provided to \code{...} of \code{\link[=simulate_mab]{simulate_mab()}} to \code{\link[furrr:furrr_options]{furrr::furrr_options()}} and the user specified \code{time_model}
}
\seealso{
Other param:
\code{\link[=generate_assignment_dates]{generate_assignment_dates()}},
\code{\link[=generate_groups]{generate_groups()}},
\code{\link[=generate_outcomes]{generate_outcomes()}},
\code{\link[=generate_period_idx]{generate_period_idx()}},
\code{\link[=prep_sim_data]{prep_sim_data()}},
\code{\link[=setup_mab_sim]{setup_mab_sim()}}
}
\concept{param}
\keyword{internal}
