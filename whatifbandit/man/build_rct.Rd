% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis_test.R
\name{build_rct}
\alias{build_rct}
\alias{build_rct_clusters}
\alias{build_p_cols}
\alias{boot_build_p}
\alias{build_time_model_args}
\title{Build Proper Arguments for RCT Bootstrap Joint Test}
\usage{
build_rct_clusters(mab, col_names)

build_p_cols(args)

boot_build_p(data, success_col, group_col, cols)

build_time_model_args(mab, args, col_names)
}
\arguments{
\item{mab}{A \code{single_rct_mab} object.}

\item{col_names}{Named list of column name strings.}

\item{args}{The processed args list from \code{\link[=joint_base_args]{joint_base_args()}}.}

\item{data}{Input data.}

\item{success_col}{Name of the success column.}

\item{group_col}{Column to group by, or \code{NULL} for the whole dataset.}

\item{cols}{Named numeric vector of column proportions (from \code{\link[=build_p_cols]{build_p_cols()}}).}
}
\value{
A named numeric vector, named list of vectors, or \code{NULL}.

A named numeric vector of proportions, or a scalar \code{1}.

A list with elements \code{cols}, \code{s} (successes), and \code{n} (totals),
all named and sorted consistently.

A list with elements \code{time_model} (function or \code{NULL}) and \code{args}
(list of additional arguments for the time model).
}
\description{
Build Proper Arguments for RCT Bootstrap Joint Test
}
\section{Functions}{
\itemize{
\item \code{build_rct_clusters()}: Resolves the \code{clusters} argument for a \code{single_rct_mab} bootstrap.
When both blocks and clusters are present, clusters is a named list of
per-block cluster proportion vectors (as documented in \code{\link[=simulate_mab]{simulate_mab()}}).
When only clusters are present, returns a flat named proportion vector via
\code{\link[=group_prop]{group_prop()}}. Returns \code{NULL} when no clustering was used.

\item \code{build_p_cols()}: Returns the named vector of proportions that drives the columns of the null
\code{p} matrix — clusters if present, blocks if present, or a scalar \code{1} for
the no-blocking/no-clustering case. For the blocked-and-clustered case,
clusters is a named list; this flattens it to a single named vector (since
the p matrix columns are individual clusters, not blocks).

\item \code{boot_build_p()}: Recovers successes and totals for each column group (cluster, block, or
the whole dataset), then returns them alongside the resolved column
proportions vector for use in \code{\link[=joint_boot_null]{joint_boot_null()}}.

\item \code{build_time_model_args()}: Constructs the \code{time_model} function and its associated argument list when
\code{delayed_feedback} is enabled for a \code{single_rct_mab} bootstrap. Returns a
list with \code{time_model = NULL} and \code{args = list()} when delayed feedback is
not in use.

}}
\keyword{internal}
