% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_api.R
\name{condense_results}
\alias{condense_results}
\title{Condenses results of repeated simulations.}
\usage{
condense_results(dt, keep_data, keep_models, mabs)
}
\arguments{
\item{dt}{Logical; Whether to output \code{data.table}s or \code{tibble}s. When\code{ r * number_of_periods > 100000}, \code{dt = TRUE}, even if the user passed data is not a
\code{data.table}.}

\item{keep_data}{Logical; Whether or not to keep the final data from each trial. Recommended
\code{FALSE}. When\code{ r = 1} the final data is always kept and reported.}

\item{keep_models}{Logical; Whether or not to keep the final IPW and OLS models from each trial. Recommended
\code{FALSE}. When\code{ r = 1} models are always kept and reported. Required to be \code{TRUE} to compute
arbitrary pairwise contrasts. Only utilized under clustering.}

\item{mabs}{List of outputs from repeated \code{\link[=run_mab]{run_mab()}} calls.}
}
\value{
A named list containing:
\itemize{
\item \code{final_data}: \code{tibble} or \code{data.table} containing the nested \code{tibble}s/\code{data.table}s from each trial. Only provided when \code{keep_data = TRUE}.
\item \code{bandits}: A \code{tibble} or \code{data.table} containing the UCB1 values or Thompson Sampling posterior distributions for each period and trial. Wide format,
each row is a period, and each column is a treatment.
\item \code{assignment_probs}: A \code{tibble} or \code{data.table} containing the probability of being assigned each treatment arm at a given period and trial. Wide format,
each row is a period, and each column is a treatment.
\item \code{assignment_quantities}: A \code{tibble} or \code{data.table} containing total number of observations assigned to each
treatment arm in each trial.
each treatment arm in each trial.
\item \code{contrasts}: A \code{tibble} or \code{data.table} containing the contrast estimates of the specified estimators for
the specified contrast structure for each trial.
\item \code{f_stats}: Named \code{tibble} or \code{data.table} containing f_stat from IPW and OLS regressions
for each trial.
\item \code{models}: A nested list containing the \code{lm_robust} objects from regressions, only saved
in clustered case, and when \code{keep_models = TRUE}
}
}
\description{
Condenses results of repeated simulations.
}
\details{
This function iterates over every element in \code{mabs} and extracts the required element to place in a condensed list
for the final output.
}
\keyword{internal}
