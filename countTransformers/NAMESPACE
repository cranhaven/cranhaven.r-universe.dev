export(
  getJaccard,
  lTransformer,
  lvTransformer,
  l2Transformer,
  lv2Transformer,
  rTransformer,
  rvTransformer,
  r2Transformer,
  rv2Transformer,
  wilcoxWrapper
)

importFrom(MASS,
          ginv)

importFrom(graphics,
          hist,
          legend,
          lines,
          plot,
          title)

importFrom(stats,
          cor,
          dnorm,
          qqnorm,
          sd,
          t.test,
          var,
          wilcox.test,
          model.matrix)

importFrom("stats", "optim")
importFrom("stats", "median", "pchisq", "pf")
importFrom("stats", "optimize")
importFrom("stats", "rchisq", "rgamma", "rnorm", "rpois")

import(Biobase)
import(limma)
