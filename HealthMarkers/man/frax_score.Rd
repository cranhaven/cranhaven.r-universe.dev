% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frax_score.R
\name{frax_score}
\alias{frax_score}
\title{FRAX 10-Year Fracture Risk Score (simplified placeholder)}
\usage{
frax_score(
  data,
  col_map = NULL,
  na_action = c("keep", "omit", "error", "ignore", "warn"),
  country = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame or tibble with inputs.}

\item{col_map}{Named list mapping required and optional inputs:
\itemize{
\item Required: age, sex
\item Optional binary risk factors: prior_fracture, parent_fracture, steroids,
rheumatoid, secondary_op, smoker, alcohol
\item Optional: bmd (T-score; if present and in (-5,0], used to adjust risk)
}}

\item{na_action}{One of c("keep","omit","error","ignore","warn").}

\item{country}{optional country/region code for FRAX calibration (accepted, currently unused).}

\item{verbose}{Logical; if TRUE, emits progress via rlang::inform.}
}
\value{
Tibble with frax_major_percent and frax_hip_percent.
}
\description{
Estimates the 10-year probabilities of major osteoporotic and hip fracture
using a simplified, non-validated approximation based on FRAX risk factors.
This is for development/demo only and does not implement the proprietary FRAX algorithm.
}
\details{
Important: this function is an educational placeholder and must not be used for
clinical decision-making, patient counseling, or guideline-based treatment selection.
}
\examples{
df <- data.frame(Age = c(65, 72, 58), Sex = c("female", "female", "male"))
frax_score(df)
}
