% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psych_scores.R
\name{k6_score}
\alias{k6_score}
\title{K6 scoring}
\usage{
k6_score(
  data,
  col_map = list(),
  na_action = c("keep", "omit", "error"),
  missing_prop_max = 0.2,
  impute = c("none", "mean"),
  prefix = "K6",
  cutoff = 13,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame containing questionnaire item columns.}

\item{col_map}{Named list mapping canonical item IDs to column names; defaults assume items are already named.}

\item{na_action}{How to handle rows with missing items: \code{keep}, \code{omit}, or \code{error}.}

\item{missing_prop_max}{Maximum allowed proportion of missing items per row before the score is set to \code{NA}.}

\item{impute}{Imputation strategy for missing items when under the threshold: \code{none} or \code{mean} (row-wise mean).}

\item{prefix}{Prefix for output column names.}

\item{cutoff}{Threshold for the K6 case flag.}

\item{verbose}{Logical; if \code{TRUE}, emits informational messages about column resolution and scoring progress via \code{hm_inform()}.}
}
\value{
A tibble of score columns only: \code{K6_total} and \code{K6_case}. Input columns are not included.
}
\description{
K6 scoring
}
\examples{
df <- data.frame(k6_01 = 0, k6_02 = 1, k6_03 = 2, k6_04 = 1, k6_05 = 0, k6_06 = 1)
k6_score(df)
}
\references{
\insertRef{kessler2002k6k10}{HealthMarkers}
\insertRef{prochaska2012k6}{HealthMarkers} (validation study)
}
