% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psych_scores.R
\name{asrs_score}
\alias{asrs_score}
\title{Adult ADHD Self-Report Scale scoring}
\usage{
asrs_score(
  data,
  col_map = list(),
  na_action = c("keep", "omit", "error"),
  missing_prop_max = 0.2,
  impute = c("none", "mean"),
  prefix = "ASRS",
  partA_items = sprintf("asrs_\%02d", 1:6),
  partA_thresholds = c(3, 3, 3, 4, 4, 4),
  partA_cutoff = 4,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame containing questionnaire item columns.}

\item{col_map}{Named list mapping canonical item IDs to column names; defaults assume items are already named.}

\item{na_action}{How to handle rows with missing items: \code{keep}, \code{omit}, or \code{error}.}

\item{missing_prop_max}{Maximum allowed proportion of missing items per row before the score is set to \code{NA}.}

\item{impute}{Imputation strategy for missing items when under the threshold: \code{none} or \code{mean} (row-wise mean).}

\item{prefix}{Prefix for output column names.}

\item{partA_items}{Vector of Part A item IDs.}

\item{partA_thresholds}{Numeric thresholds applied to Part A items.}

\item{partA_cutoff}{Count threshold for Part A positivity.}

\item{verbose}{Logical; if \code{TRUE}, emits informational messages about column resolution and scoring progress via \code{hm_inform()}.}
}
\value{
A tibble of score columns only: \code{ASRS_total}, \code{ASRS_partA_count}, \code{ASRS_partA_positive}. Input columns are not included.
}
\description{
Adult ADHD Self-Report Scale scoring
}
\note{
Items are expected on a 0--4 scale (Never=0, Rarely=1, Sometimes=2,
Often=3, Very Often=4). The default \code{partA_thresholds} follow the
official WHO ASRS v1.1 guide: items 1--3 are positive at \eqn{\geq 3}
(Often or Very Often) and items 4--6 at \eqn{\geq 4} (Very Often only).
}
\examples{
df <- data.frame(matrix(2, nrow = 1, ncol = 18))
names(df) <- sprintf("asrs_\%02d", 1:18)
asrs_score(df)
}
\references{
\insertRef{adler2006asrs}{HealthMarkers}
\insertRef{kessler2007asrs}{HealthMarkers} (prevalence study; ASRS used as instrument)
}
