% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psych_scores.R
\name{mdq_score}
\alias{mdq_score}
\title{Mood Disorder Questionnaire scoring}
\usage{
mdq_score(
  data,
  col_map = list(),
  na_action = c("keep", "omit", "error"),
  missing_prop_max = 0.2,
  prefix = "MDQ",
  symptom_cutoff = 7,
  require_clustering = TRUE,
  require_impairment = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame containing questionnaire item columns.}

\item{col_map}{Named list mapping canonical item IDs to column names; defaults assume items are already named.}

\item{na_action}{How to handle rows with missing items: \code{keep}, \code{omit}, or \code{error}.}

\item{missing_prop_max}{Maximum allowed proportion of missing items per row before the score is set to \code{NA}.}

\item{prefix}{Prefix for output column names.}

\item{symptom_cutoff}{Minimum symptom count for a positive screen.}

\item{require_clustering}{Require clustering item == 1 to be positive.}

\item{require_impairment}{Require impairment item == 1 to be positive.}

\item{verbose}{Logical; if \code{TRUE}, emits informational messages about column resolution and scoring progress via \code{hm_inform()}.}
}
\value{
A tibble of score columns only: \code{MDQ_symptom_count}, \code{MDQ_clustering}, \code{MDQ_impairment}, \code{MDQ_positive_screen}. Input columns are not included.
}
\description{
Mood Disorder Questionnaire scoring
}
\note{
The impairment column is expected to be \strong{binary} (1 = impaired, 0 = not).
The original MDQ impairment question uses a 4-category scale
(1 = no problem, 2 = minor, 3 = moderate, 4 = serious); if the raw
4-category response is passed, the caller must recode it to binary
(e.g., \code{impair_binary = as.integer(impair_raw >= 3)}) before scoring.
}
\examples{
df <- data.frame(matrix(0, nrow = 1, ncol = 13))
names(df) <- sprintf("mdq_\%02d", 1:13)
df$mdq_cluster <- 1; df$mdq_impair <- 1
mdq_score(df)
}
\references{
\insertRef{hirschfeld2000mdq}{HealthMarkers}
}
