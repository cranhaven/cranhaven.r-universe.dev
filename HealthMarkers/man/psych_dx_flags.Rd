% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psych_scores.R
\name{psych_dx_flags}
\alias{psych_dx_flags}
\title{Psychiatric diagnosis flags aggregator}
\usage{
psych_dx_flags(
  data,
  col_map = list(),
  prefix = "dx",
  na_action = c("keep", "omit", "error")
)
}
\arguments{
\item{data}{Data frame containing questionnaire item columns.}

\item{col_map}{Named list \code{dx} mapping condition ids (e.g., mdd, bipolar) to columns of boolean/numeric flags.}

\item{prefix}{Prefix for output flag columns.}

\item{na_action}{How to handle rows with missing items: \code{keep}, \code{omit}, or \code{error}.}
}
\value{
A tibble of flag columns only: \code{dx_any_psych}, \code{dx_internalizing}, \code{dx_externalizing}, \code{dx_psychotic}, \code{dx_count}. Input columns are not included.
}
\description{
Psychiatric diagnosis flags aggregator
}
\examples{
df <- data.frame(dx_mdd = c(1, 0), dx_bipolar = c(0, 1))
psych_dx_flags(df, col_map = list(dx = list(mdd = "dx_mdd", bipolar = "dx_bipolar")))
}
