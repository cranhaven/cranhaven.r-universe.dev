# Dependencies
import(DBI)
import(RJDBC)
import(methods)
importFrom(utils, "flush.console", "tail", "unzip")

# Classes
exportClass(ODB)
exportClass(progress, progress.console, progress.file)

# Functions
export(odb.create, odb.open, odb.close, isClosed)
export(odb.read, odb.export, odb.write, odb.insert)
export(odb.tables, odb.comments, "odb.comments<-", odb.queries, "odb.queries<-")

# Tools
export(difftimeFmt, set)
