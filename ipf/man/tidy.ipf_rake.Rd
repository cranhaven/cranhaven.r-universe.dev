% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.ipf_rake}
\alias{tidy.ipf_rake}
\title{Tidy an ipf_rake object}
\usage{
\method{tidy}{ipf_rake}(x, ...)
}
\arguments{
\item{x}{An \code{ipf_rake} object.}

\item{...}{Additional arguments (ignored).}
}
\value{
A tibble with columns: \code{variable}, \code{level}, \code{target}, \code{weighted_pct}, \code{discrepancy}.
}
\description{
Returns a one-row-per-variable-per-level tibble with target proportions, weighted proportions, and discrepancy.
}
\examples{
data <- data.frame(
  gender = sample(c('M', 'F'), 100, replace = TRUE, prob = c(0.6, 0.4))
)
targets <- list(gender = c(M = 0.5, F = 0.5))
result <- rake(data, targets)
tidy(result)

}
