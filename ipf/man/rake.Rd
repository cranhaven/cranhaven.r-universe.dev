% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rake.R
\name{rake}
\alias{rake}
\title{Iterative proportional fitting (raking)}
\usage{
rake(
  data,
  targets,
  base_weights = NULL,
  cap = 5,
  bounds = NULL,
  type = c("nolim", "pctlim", "nlim"),
  pctlim = 0.05,
  nlim = 5L,
  choosemethod = c("total", "max", "average", "totalsquared", "maxsquared",
    "averagesquared"),
  na_method = c("exclude", "bucket"),
  iterate = TRUE,
  max_iter = 1000L,
  tol = 1e-06,
  verbose = FALSE,
  diagnostics_every = 0L
)
}
\arguments{
\item{data}{A data frame or tibble containing the survey data.}

\item{targets}{A named list of named numeric vectors specifying target proportions for each raking variable.
Names of the list must match column names in \code{data}.
Each vector's names must match the levels of the corresponding variable.
Values should sum to 1 (proportions); if not, they are normalized with a warning.}

\item{base_weights}{Optional numeric vector of base (design) weights.
If \code{NULL} (default), uniform weights of 1 are used.
Centered to mean 1 before raking.}

\item{cap}{Maximum weight value (ratio cap).
Weights exceeding this value are trimmed and all weights are renormalized.
Default \code{5}.
Ignored if \code{bounds} is specified.}

\item{bounds}{Optional numeric vector of length 2, \code{c(lo, hi)}, specifying minimum and maximum weight bounds.
Overrides \code{cap}.}

\item{type}{Variable selection method:
\itemize{
\item \code{"nolim"} (default): use all variables in \code{targets}.
\item \code{"pctlim"}: use only variables with discrepancy >= \code{pctlim}.
\item \code{"nlim"}: use the \code{nlim} most discrepant variables.
}}

\item{pctlim}{Discrepancy threshold for \code{type = "pctlim"}.
Default \code{0.05} (5 percentage points).}

\item{nlim}{Number of variables for \code{type = "nlim"}.
Default \code{5}.}

\item{choosemethod}{Method for aggregating per-category discrepancies into a single variable score.
One of \code{"total"}, \code{"max"}, \code{"average"}, \code{"totalsquared"}, \code{"maxsquared"}, \code{"averagesquared"}.}

\item{na_method}{How to handle \code{NA} values in raking variables.
\code{"exclude"} (default): targets are proportions among non-NA cases only; NA cases are
invisible to that margin. Matches anesrake.
\code{"bucket"}: NAs become a frozen extra category; their total weight is preserved and the
named targets are rescaled to the remaining non-NA mass.}

\item{iterate}{Logical.
If \code{TRUE} and \code{type = "pctlim"}, re-check discrepancies after raking and add newly discrepant variables, repeating up to 10 times.
Default \code{TRUE}.}

\item{max_iter}{Maximum number of raking iterations.
Default \code{1000}.}

\item{tol}{Convergence tolerance (max proportional error).
Default \code{1e-6}.}

\item{verbose}{Logical.
If \code{TRUE}, print iteration progress.
Default \code{FALSE}.}

\item{diagnostics_every}{Record per-margin diagnostics every \code{k} iterations.
\code{0} means only baseline.
Default \code{0}.}
}
\value{
An \code{ipf_rake} object (S3 class) containing:
\itemize{
\item \code{weights}: final raked weight vector
\item \code{data}: the input data frame
\item \code{converged}: logical
\item \code{iterations}: number of iterations
\item \code{max_prop_err}: final max proportional error
\item \code{targets}: normalized targets used
\item \code{vars_used}: character vector of variables raked on
\item \code{base_weights}: original base weights
\item \code{type}, \code{choosemethod}, \code{na_method}, \code{cap}: settings used
\item \code{deff}, \code{n_eff}: design effect and effective sample size
\item \code{diagnostics}: tibble of per-iteration diagnostics
}
}
\description{
Adjusts survey weights so that weighted marginal distributions match known population targets.
Supports automatic variable selection, iterative re-raking, and weight bounding.
}
\examples{
data <- data.frame(
  gender = sample(c('M', 'F'), 100, replace = TRUE, prob = c(0.6, 0.4)),
  age = sample(c('young', 'old'), 100, replace = TRUE, prob = c(0.7, 0.3))
)
targets <- list(
  gender = c(M = 0.5, F = 0.5),
  age = c(young = 0.6, old = 0.4)
)
result <- rake(data, targets)
print(result)
}
