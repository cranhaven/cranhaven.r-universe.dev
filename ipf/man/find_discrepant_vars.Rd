% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrepancy.R
\name{find_discrepant_vars}
\alias{find_discrepant_vars}
\title{Find discrepant variables and their aggregate discrepancy scores}
\usage{
find_discrepant_vars(
  data,
  targets,
  weights,
  choosemethod = "total",
  na_method = c("exclude", "bucket")
)
}
\arguments{
\item{data}{Data frame.}

\item{targets}{Named list of named numeric target vectors (proportions).}

\item{weights}{Numeric weight vector.}

\item{choosemethod}{Method for aggregating per-category discrepancies.
One of \code{"total"}, \code{"max"}, \code{"average"}, \code{"totalsquared"}, \code{"maxsquared"}, \code{"averagesquared"}.}

\item{na_method}{How to handle \code{NA} values.
\code{"exclude"} skips NA cases from that margin.
\code{"bucket"} treats missing values as an implicit extra category.}
}
\value{
Named numeric vector of aggregate discrepancy per variable.
}
\description{
Calculates discrepancy between the current weighted distribution and target distributions for each variable, then aggregates using the chosen method.
}
\examples{
data <- data.frame(
  gender = sample(c('M', 'F'), 100, replace = TRUE, prob = c(0.6, 0.4)),
  age = sample(c('young', 'old'), 100, replace = TRUE, prob = c(0.7, 0.3))
)
targets <- list(
  gender = c(M = 0.5, F = 0.5),
  age = c(young = 0.6, old = 0.4)
)
find_discrepant_vars(data, targets, weights = rep(1, 100))
}
