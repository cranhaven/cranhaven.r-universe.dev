% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{weight_assess}
\alias{weight_assess}
\title{Assess weight quality with diagnostic tables}
\usage{
weight_assess(
  data,
  targets,
  weights,
  base_weights = NULL,
  na_method = c("exclude", "bucket")
)
}
\arguments{
\item{data}{Data frame.}

\item{targets}{Named list of named numeric target vectors (proportions).}

\item{weights}{Final raked weight vector.}

\item{base_weights}{Original base weights before raking.
If \code{NULL}, uses uniform weights.}

\item{na_method}{How to handle \code{NA} values.
\code{"exclude"} skips NA cases from that margin.
\code{"bucket"} treats missing values as an implicit extra category.}
}
\value{
Named list of tibbles, one per variable.
}
\description{
Produces a per-variable diagnostic table comparing target distributions to unweighted and weighted distributions.
}
\examples{
data <- data.frame(
  gender = sample(c('M', 'F'), 100, replace = TRUE, prob = c(0.6, 0.4))
)
targets <- list(gender = c(M = 0.5, F = 0.5))
result <- rake(data, targets)
weight_assess(data, targets, result$weights)
}
