% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_stl}
\alias{export_stl}
\title{Export a DEM to STL for 3D Printing}
\usage{
export_stl(dem, output, exaggeration = 1, base_height = 1, decimate = 1L)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the terrain surface.}

\item{output}{Character. Output file path (should end in \code{.stl}).}

\item{exaggeration}{Numeric. Vertical exaggeration factor.
Default \code{1} (no exaggeration). Use \code{2} for 2x vertical stretch.}

\item{base_height}{Numeric. Thickness of the flat base below the
terrain minimum, in DEM units. Default \code{1}.}

\item{decimate}{Integer. Keep every Nth cell. Default \code{1}.}
}
\value{
The output file path (invisibly).
}
\description{
Converts a DEM raster to an STL (stereolithography) file suitable
for 3D printing. The model includes a flat base and vertical
exaggeration control.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
out <- file.path(tempdir(), "terrain.stl")
export_stl(dem, out, exaggeration = 2)
file.exists(out)
}
