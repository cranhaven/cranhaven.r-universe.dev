% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_landxml}
\alias{export_landxml}
\title{Export a DEM to LandXML TIN Surface}
\usage{
export_landxml(
  dem,
  output,
  surface_name = "Ground",
  decimate = 1L,
  boundary = NULL
)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the terrain surface.}

\item{output}{Character. Output file path (should end in \code{.xml}).}

\item{surface_name}{Character. Name for the TIN surface in the XML.
Default \code{"Ground"}.}

\item{decimate}{Integer. Keep every Nth point to reduce file size.
Default \code{1} (keep all points). Use \code{2} to halve the point count,
\code{5} to keep every 5th point, etc.}

\item{boundary}{An optional \link[sf:sf]{sf::sf} polygon to clip the DEM before export.}
}
\value{
The output file path (invisibly).
}
\description{
Converts a DEM raster to a LandXML file containing a TIN (Triangulated
Irregular Network) surface. LandXML is widely supported by civil
engineering software including Autodesk Civil 3D, Bentley OpenRoads,
and Trimble Business Center.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
out <- file.path(tempdir(), "surface.xml")
export_landxml(dem, out, surface_name = "Existing")
file.exists(out)
}
