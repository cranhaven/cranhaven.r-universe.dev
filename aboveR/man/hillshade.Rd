% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terrain.R
\name{hillshade}
\alias{hillshade}
\title{Generate a Hillshade from a DEM}
\usage{
hillshade(dem, azimuth = 315, altitude = 45)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the terrain surface.}

\item{azimuth}{Numeric. Sun azimuth in degrees (compass bearing).
Default \code{315} (northwest).}

\item{altitude}{Numeric. Sun altitude angle in degrees above the
horizon. Default \code{45}.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with hillshade values (0--255).
}
\description{
Computes an analytical hillshade (shaded relief) from a DEM using
specified sun azimuth and altitude angles. Useful for terrain
visualisation and map backgrounds.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
hs <- hillshade(dem)
terra::plot(hs, col = grey.colors(256))
}
