% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reclamation.R
\name{reclamation_progress}
\alias{reclamation_progress}
\title{Assess Reclamation Progress Between Time Steps}
\usage{
reclamation_progress(current, target, boundary = NULL, tolerance = 0.3)
}
\arguments{
\item{current}{A \link[terra:SpatRaster-class]{terra::SpatRaster} of the current terrain surface.}

\item{target}{A \link[terra:SpatRaster-class]{terra::SpatRaster} of the target / design grade surface.}

\item{boundary}{An optional \link[sf:sf]{sf::sf} polygon to restrict analysis to.}

\item{tolerance}{Numeric. Cells within +/- \code{tolerance} of the target
are considered "on grade". Default \code{0.3} (metres).}
}
\value{
A list with:
\itemize{
\item \code{deviation}: \link[terra:SpatRaster-class]{terra::SpatRaster} of current - target values
\item \code{on_grade_pct}: percentage of cells within tolerance
\item \code{above_grade_pct}: percentage of cells above tolerance
\item \code{below_grade_pct}: percentage of cells below tolerance
\item \code{mean_deviation}: mean signed deviation
\item \code{rmse}: root mean square error
}
}
\description{
Compares a current DEM against a target (design grade) surface to
quantify how much of a reclamation area has been restored to the
desired elevation. Returns per-cell deviations and summary statistics.
}
\examples{
current <- terra::rast(system.file("extdata/dem_after.tif", package = "aboveR"))
target  <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
prog <- reclamation_progress(current, target, tolerance = 1)
cat("On grade:", prog$on_grade_pct, "\%\n")
}
