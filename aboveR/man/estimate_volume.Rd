% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volume.R
\name{estimate_volume}
\alias{estimate_volume}
\title{Estimate Cut or Fill Volume Between Two Surfaces}
\usage{
estimate_volume(
  surface,
  reference,
  boundary = NULL,
  method = c("trapezoidal", "simpson")
)
}
\arguments{
\item{surface}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the actual surface.}

\item{reference}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or single numeric value
representing the reference elevation. If numeric, a constant
reference plane at that elevation is used.}

\item{boundary}{An optional \link[sf:sf]{sf::sf} polygon to clip both surfaces to
before computation.}

\item{method}{Character. Volume integration method: \code{"trapezoidal"}
(default) or \code{"simpson"}. Trapezoidal sums \code{abs(diff) * cell_area}.
Simpson uses Simpson's 1/3 rule for higher accuracy.}
}
\value{
A list with components:
\itemize{
\item \code{cut_volume_m3}: volume of material removed (positive)
\item \code{fill_volume_m3}: volume of material added (positive)
\item \code{net_volume_m3}: fill minus cut
\item \code{area_m2}: total analysed area
\item \code{mean_depth_m}: mean absolute difference
\item \code{max_cut_m}: deepest cut
\item \code{max_fill_m}: highest fill
\item \code{method}: integration method used
}
}
\description{
Computes the volume of material between a \code{surface} DEM and a
\code{reference} surface (e.g., a design grade or pre-mining DEM),
optionally clipped to a \code{boundary} polygon. Reports cut and fill
volumes separately and documents the integration method used.
}
\examples{
surface <- terra::rast(system.file("extdata/dem_after.tif", package = "aboveR"))
reference <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
boundary <- sf::st_read(
  system.file("extdata/boundary.gpkg", package = "aboveR"),
  quiet = TRUE
)
vol <- estimate_volume(surface, reference, boundary)
cat("Net volume:", vol$net_volume_m3, "m3\n")
}
