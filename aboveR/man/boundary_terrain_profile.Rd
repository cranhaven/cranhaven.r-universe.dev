% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{boundary_terrain_profile}
\alias{boundary_terrain_profile}
\title{Extract Terrain Profile Along a Polygon Boundary}
\usage{
boundary_terrain_profile(dem, boundary, spacing = NULL)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the terrain surface.}

\item{boundary}{An \link[sf:sf]{sf::sf} polygon whose boundary (exterior ring) will
be profiled.}

\item{spacing}{Numeric. Distance between sample points, in CRS units
of \code{dem}. Default \code{NULL} uses 1 cell width.}
}
\value{
A data frame with columns:
\itemize{
\item \code{distance}: distance along the boundary perimeter
\item \code{elevation}: sampled elevation value
\item \code{x}, \code{y}: coordinates of each sample point
}
}
\description{
Converts a polygon boundary to a closed linestring and samples
elevation values around the perimeter. Useful for inspecting
highwall edges, dam crests, or property boundaries.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
boundary <- sf::st_read(
  system.file("extdata/boundary.gpkg", package = "aboveR"),
  quiet = TRUE
)
bprof <- boundary_terrain_profile(dem, boundary)
plot(bprof$distance, bprof$elevation, type = "l",
     xlab = "Perimeter Distance", ylab = "Elevation")
}
