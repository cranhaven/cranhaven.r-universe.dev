% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{terrain_profile}
\alias{terrain_profile}
\title{Extract a Terrain Profile Along a Line}
\usage{
terrain_profile(dem, line, spacing = NULL)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the terrain surface.}

\item{line}{An \link[sf:sf]{sf::sf} object containing a single LINESTRING geometry
defining the transect. Or a path to a GeoPackage/shapefile.}

\item{spacing}{Numeric. Distance between sample points along the
line, in the CRS units of \code{dem}. Default \code{NULL} uses 1 cell width.}
}
\value{
A data frame with columns:
\itemize{
\item \code{distance}: distance along the profile from the start point
\item \code{elevation}: sampled elevation value
\item \code{x}, \code{y}: coordinates of each sample point
}
}
\description{
Samples elevation values from a DEM at regular intervals along a
transect line and returns a data frame of distance vs. elevation.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
line <- sf::st_read(
  system.file("extdata/profile_line.gpkg", package = "aboveR"),
  quiet = TRUE
)
prof <- terrain_profile(dem, line)
plot(prof$distance, prof$elevation, type = "l",
     xlab = "Distance", ylab = "Elevation")
}
