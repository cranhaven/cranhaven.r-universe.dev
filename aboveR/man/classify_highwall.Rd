% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highwall.R
\name{classify_highwall}
\alias{classify_highwall}
\title{Classify Highwall Areas from a DEM}
\usage{
classify_highwall(dem, slope_threshold = 60, min_area = 0, as_polygons = FALSE)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the terrain surface.}

\item{slope_threshold}{Numeric. Minimum slope in degrees to classify
as highwall. Default \code{60}.}

\item{min_area}{Numeric. Minimum contiguous area (in map units squared)
for a highwall zone. Smaller patches are removed. Default \code{0}
(keep all).}

\item{as_polygons}{Logical. Return vectorised polygons instead of a
raster? Default \code{FALSE}.}
}
\value{
If \code{as_polygons = FALSE}, a \link[terra:SpatRaster-class]{terra::SpatRaster} with values
1 (highwall) and \code{NA} (non-highwall). If \code{as_polygons = TRUE}, an
\link[sf:sf]{sf::sf} data frame of highwall polygons with an \code{area_m2} column.
}
\description{
Identifies steep terrain faces (highwalls) typical of surface mining
operations by computing slope from a DEM and classifying cells that
exceed a slope threshold. Returns a binary raster and optionally
vectorised polygons of highwall zones.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
hw <- classify_highwall(dem, slope_threshold = 5)
terra::plot(hw)
}
