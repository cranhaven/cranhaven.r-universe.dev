% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/change.R
\name{change_by_zone}
\alias{change_by_zone}
\title{Summarise Terrain Change by Zone}
\usage{
change_by_zone(change_raster, zones, id_field)
}
\arguments{
\item{change_raster}{A \link[terra:SpatRaster-class]{terra::SpatRaster} as returned by \code{\link[=terrain_change]{terrain_change()}}
(uses the \code{"change"} layer).}

\item{zones}{An \link[sf:sf]{sf::sf} data frame of polygons defining analysis zones.}

\item{id_field}{Character. Column name in \code{zones} to use as zone identifier.}
}
\value{
An \link[sf:sf]{sf::sf} data frame with columns:
\itemize{
\item zone identifier
\item \code{cut_volume}: total volume of material removed (m^3, positive)
\item \code{fill_volume}: total volume of material added (m^3, positive)
\item \code{net_volume}: fill - cut (m^3)
\item \code{area_m2}: zone area
\item \code{mean_change}: mean elevation change
\item \code{max_cut}: deepest cut (most negative value)
\item \code{max_fill}: highest fill (most positive value)
}
}
\description{
Extracts change statistics for each polygon in a zone layer,
computing cut volume, fill volume, net change, and descriptive
statistics per zone.
}
\examples{
before <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
after  <- terra::rast(system.file("extdata/dem_after.tif", package = "aboveR"))
chg <- terrain_change(before, after)
zones <- sf::st_read(
  system.file("extdata/zones.gpkg", package = "aboveR"),
  quiet = TRUE
)
change_by_zone(chg, zones, id_field = "zone_id")
}
