% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volume.R
\name{impoundment_curve}
\alias{impoundment_curve}
\title{Generate an Impoundment Capacity Curve}
\usage{
impoundment_curve(dem, boundary = NULL, elevations = NULL, n_steps = 20L)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the terrain surface.}

\item{boundary}{An \link[sf:sf]{sf::sf} polygon defining the impoundment boundary
(e.g., dam crest outline). If \code{NULL}, the full DEM extent is used.}

\item{elevations}{Numeric vector of water surface elevations to
evaluate. If \code{NULL}, a sequence from the minimum to maximum DEM
value within the boundary is generated with \code{n_steps} increments.}

\item{n_steps}{Integer. Number of elevation increments when
\code{elevations} is \code{NULL}. Default 20.}
}
\value{
A data frame with columns:
\itemize{
\item \code{elevation}: water surface elevation
\item \code{area_m2}: inundated area at this elevation
\item \code{volume_m3}: cumulative storage volume below this elevation
}
}
\description{
Calculates storage volume at a series of water surface elevations
for a terrain depression (e.g., a pond, reservoir, or sediment basin).
The result is an elevation-area-volume curve.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
boundary <- sf::st_read(
  system.file("extdata/boundary.gpkg", package = "aboveR"),
  quiet = TRUE
)
curve <- impoundment_curve(dem, boundary, n_steps = 10)
plot(curve$elevation, curve$volume_m3, type = "l",
     xlab = "Elevation", ylab = "Volume (m3)")
}
