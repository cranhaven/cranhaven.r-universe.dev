% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terrain.R
\name{contour_lines}
\alias{contour_lines}
\title{Generate Contour Lines from a DEM}
\usage{
contour_lines(dem, interval = 5, levels = NULL)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the terrain surface.}

\item{interval}{Numeric. Elevation interval between contour lines.
Default \code{5}.}

\item{levels}{Numeric vector. Specific elevations to contour. If
provided, \code{interval} is ignored.}
}
\value{
An \link[sf:sf]{sf::sf} data frame with LINESTRING geometry and an
\code{elevation} column.
}
\description{
Extracts contour lines at a specified interval and returns them as
an \link[sf:sf]{sf::sf} object with an \code{elevation} attribute.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
cl <- contour_lines(dem, interval = 2)
plot(sf::st_geometry(cl))
}
