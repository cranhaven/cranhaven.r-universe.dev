% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kfa_tiles.R
\name{kfa_tile_index}
\alias{kfa_tile_index}
\title{Load and Cache a KyFromAbove Tile Index}
\usage{
kfa_tile_index(product = "dem", phase = 2L, max_age_days = 30L)
}
\arguments{
\item{product}{Character. One of \code{"dem"}, \code{"pointcloud"}, \code{"ortho"}.}

\item{phase}{Integer. KyFromAbove acquisition phase: 1, 2, or 3.}

\item{max_age_days}{Integer. Re-download if cache is older than this. Default 30.}
}
\value{
An \link[sf:sf]{sf::sf} data frame representing the tile index grid.
}
\description{
Downloads a tile index GeoPackage from the KyFromAbove S3 bucket
and caches it locally. Subsequent calls use the cached copy unless
it is older than \code{max_age_days}.
}
\examples{
\dontshow{if (aboveR::has_s3_access()) withAutoprint(\{ # examplesIf}
idx <- kfa_tile_index(product = "dem", phase = 2)
head(idx)
\dontshow{\}) # examplesIf}
}
