% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/change.R
\name{terrain_change}
\alias{terrain_change}
\title{Compute Terrain Change Between Two DEMs}
\usage{
terrain_change(before, after, tolerance = 0.1)
}
\arguments{
\item{before}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the earlier DEM.}

\item{after}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the later DEM.}

\item{tolerance}{Numeric. Changes within +/- \code{tolerance} are classified
as stable. Default \code{0.1} (metres or native units).}
}
\value{
A two-layer \link[terra:SpatRaster-class]{terra::SpatRaster}:
\itemize{
\item \code{change}: continuous elevation difference (after - before)
\item \code{class}: integer classification (1 = cut, 2 = stable, 3 = fill)
}
}
\description{
Calculates the elevation difference between a \emph{before} and \emph{after} DEM,
returning both the continuous change values and a classified layer
(cut / stable / fill). Rasters are aligned automatically if their
extents or resolutions differ (same CRS required).
}
\examples{
before <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
after  <- terra::rast(system.file("extdata/dem_after.tif", package = "aboveR"))
result <- terrain_change(before, after)
terra::plot(result[["change"]])
}
