% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flood.R
\name{flood_inundation}
\alias{flood_inundation}
\title{Generate a Flood Inundation Mask}
\usage{
flood_inundation(dem, water_level, boundary = NULL)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster} representing the terrain surface.}

\item{water_level}{Numeric. Water surface elevation in the same
units as the DEM.}

\item{boundary}{An optional \link[sf:sf]{sf::sf} polygon to restrict analysis to
(e.g., a floodplain boundary).}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with values 1 (inundated) and
\code{NA} (dry).
}
\description{
Creates a binary raster showing which cells would be inundated
at a given water surface elevation. Cells below the water level
are marked as flooded (1), cells above are \code{NA}.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
inundated <- flood_inundation(dem, water_level = 305)
terra::plot(inundated, main = "Flood Inundation at 305m")
}
