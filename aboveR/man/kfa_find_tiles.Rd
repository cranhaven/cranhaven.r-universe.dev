% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kfa_tiles.R
\name{kfa_find_tiles}
\alias{kfa_find_tiles}
\title{Find KyFromAbove Tiles Covering an Area of Interest}
\usage{
kfa_find_tiles(
  aoi,
  product = "dem",
  phase = 2L,
  method = c("auto", "stac", "index")
)
}
\arguments{
\item{aoi}{An \link[sf:sf]{sf::sf} object or numeric bbox (\code{c(xmin, ymin, xmax, ymax)})
defining the area of interest. Any CRS is accepted; reprojection to
EPSG:3089 (Kentucky Single Zone) is handled internally.}

\item{product}{Character. One of \code{"dem"}, \code{"pointcloud"}, \code{"ortho"},
\code{"contours"}, \code{"oblique"}.}

\item{phase}{Integer. KyFromAbove acquisition phase: 1, 2, or 3.
Phase 1 DEMs are 5ft resolution; Phase 2/3 are 2ft.}

\item{method}{Character. \code{"auto"} (default) tries STAC first, then
tile index. \code{"stac"} uses STAC only. \code{"index"} uses tile index only.}
}
\value{
An \link[sf:sf]{sf::sf} data frame with columns: \code{tilename}, \code{s3_url}, \code{phase},
\code{product}, and \code{geometry}.
}
\description{
Queries KyFromAbove tile indexes to find elevation, point cloud,
or imagery tiles that intersect a given area of interest. Tries
the STAC catalog first (if available), then falls back to the
tile index GeoPackage on S3.
}
\examples{
\dontshow{if (aboveR::has_s3_access()) withAutoprint(\{ # examplesIf}
# Find Phase 2 DEM tiles for a bounding box in Fayette County
tiles <- kfa_find_tiles(
  aoi = c(-84.55, 37.95, -84.45, 38.05),
  product = "dem",
  phase = 2
)
\dontshow{\}) # examplesIf}
}
