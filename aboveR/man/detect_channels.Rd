% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erosion.R
\name{detect_channels}
\alias{detect_channels}
\title{Detect Erosion Channels from a DEM}
\usage{
detect_channels(dem, threshold = 100, as_lines = FALSE)
}
\arguments{
\item{dem}{A \link[terra:SpatRaster-class]{terra::SpatRaster}.}

\item{threshold}{Numeric. Minimum flow accumulation value to classify
as a channel. Default \code{100}.}

\item{as_lines}{Logical. Return channel centrelines as \link[sf:sf]{sf::sf} lines?
Default \code{FALSE} (returns raster).}
}
\value{
If \code{as_lines = FALSE}, a \link[terra:SpatRaster-class]{terra::SpatRaster} with channel
cells = 1, other = \code{NA}. If \code{as_lines = TRUE}, an \link[sf:sf]{sf::sf} LINESTRING
object of detected channels.
}
\description{
Identifies potential erosion channels (rills, gullies) by computing
flow accumulation and filtering cells where accumulated flow exceeds
a threshold. Optionally returns vectorised channel lines.
}
\examples{
dem <- terra::rast(system.file("extdata/dem_before.tif", package = "aboveR"))
channels <- detect_channels(dem, threshold = 50)
terra::plot(channels)
}
