exportPattern("^[[:alpha:]]+")
exportMethods(
    "[",
    "[<-",
    "show",
    "summary",
    "names",
    "plot"
)
exportClasses(
    "KDD",
    "KDD.yearly.average",
    "KT.output"
)
importFrom("grDevices", "gray")
importFrom("graphics", "abline", "axis", "box", "boxplot", "contour",
           "hist", "image", "lines", "mtext", "points", "polygon",
           "text")
importFrom("stats", "dist", "quantile", "rbinom", "sd", "as.formula")

import("methods")
import("maps")
import("mapdata")
import("proj4")
import("sp")
import("gstat")
import("automap")
