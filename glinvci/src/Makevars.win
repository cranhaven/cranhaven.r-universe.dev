# -*- Makefile -*-

PKG_CFLAGS=$(SHLIB_OPENMP_CFLAGS)
PKG_LIBS=$(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(SHLIB_OPENMP_CFLAGS)# `pkg-config --silence-errors --libs tcmalloc`
OBJECTS=dglinv.o phytr.o oumods.o chn.o test_ieee_fort.o test_ieee_c.o

