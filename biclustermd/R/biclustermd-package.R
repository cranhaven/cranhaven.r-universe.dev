#' biclustermd: A package to bicluster data with missing values
#'
#' The main function is `biclustermd()`. Results can be plotted with `autoplot()` 
#' and `as.Biclust()` converts results to Biclust objects.
#' 
#' @docType package
#' @name biclustermd-package
NULL