% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replicate.R
\name{calibrate_replicate_weights}
\alias{calibrate_replicate_weights}
\alias{print.replicate_calibration}
\title{Re-calibrate replicate weights}
\usage{
calibrate_replicate_weights(
  fit,
  repweights,
  scale = 1,
  rscales = NULL,
  progress = FALSE
)

\method{print}{replicate_calibration}(x, ...)
}
\arguments{
\item{fit}{A fitted object of class \code{pass_rate_calibration}.}

\item{repweights}{A numeric matrix or data frame of replicate weights with
one row per observation (matching \code{fit}) and one column per replicate. All
entries must be finite and positive.}

\item{scale, rscales}{Replication variance constants, as in
\code{survey::svrepdesign()}. \verb{Var = scale * sum_r rscales_r * (theta_r - theta_0)^2}. Set them to match your replication scheme (for example JK1:
\code{scale = (R-1)/R}, \code{rscales = 1}; bootstrap: \code{scale = 1/R}). \code{rscales}
defaults to a vector of ones.}

\item{progress}{Logical; show a text progress bar over the replicates.}

\item{x}{A \code{replicate_calibration} object (for the print method).}

\item{...}{Ignored.}
}
\value{
An object of class \code{replicate_calibration} with the full-sample
calibrated \code{weights}, the matrix of calibrated \code{replicate_weights}, and the
\code{scale}/\code{rscales} constants.
}
\description{
Re-runs the calibration of a fitted \code{pass_rate_calibration} object for each
set of replicate weights (e.g. bootstrap, jackknife or BRR weights produced
elsewhere), holding the targets and solver settings fixed. The resulting
calibrated replicate weights feed \code{\link[=replicate_variance]{replicate_variance()}} for design-consistent
variance estimation, mirroring the replicate-weights approach of the \code{survey}
package.
}
\examples{
d <- example_rate_data(300)
fit <- calibrate_rates(d, "qualified", "initial_weight",
                       groups = list(sex = c(M = 0.72, F = 0.68)))
repw <- d$initial_weight * matrix(stats::runif(nrow(d) * 5, 0.8, 1.2), ncol = 5)
rc <- calibrate_replicate_weights(fit, repw)
replicate_variance(rc, d$qualified, statistic = "mean")
}
\seealso{
\code{\link[=replicate_variance]{replicate_variance()}}
}
