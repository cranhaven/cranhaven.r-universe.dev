% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_pass_rates.R
\name{calibrate_pass_rates}
\alias{calibrate_pass_rates}
\title{Calibrate weights to multiple pass-rate targets}
\usage{
calibrate_pass_rates(
  data,
  outcome,
  weight,
  group_vars,
  targets,
  lower = 0.25,
  upper = 4,
  mode = c("soft", "exact"),
  distance = c("chi2", "raking", "logit"),
  lambda = 10000,
  new_weight = "weight_calibrated",
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame containing one row per sampled unit.}

\item{outcome}{Name of a binary 0/1 outcome column.}

\item{weight}{Name of the initial positive weight column.}

\item{group_vars}{Character vector naming grouping variables.}

\item{targets}{A data frame with columns \code{variable}, \code{level}, and
\code{target_rate}; optional column \code{priority} controls soft-mode importance.}

\item{lower, upper}{Scalar lower and upper bounds on the multiplier applied
to each initial cell weight.}

\item{mode}{Either \code{"soft"} or \code{"exact"}.}

\item{distance}{Distance function of the calibration family. \code{"chi2"} (the
default) is the linear/quadratic distance solved as a bounded QP via OSQP
and reproduces earlier behaviour. \code{"raking"} is the entropy distance
\verb{g log g - g + 1}, whose solution \code{g = exp(eta)} is strictly positive by
construction; it is solved by a dual Newton iteration and currently
supports \code{mode = "exact"} only. Raking is unbounded above, so \code{lower} and
\code{upper} are not enforced for it but multipliers outside that range are
reported in the diagnostics. \code{"logit"} is the bounded logit distance whose
multipliers stay strictly inside \verb{(lower, upper)} by construction (requires
\verb{lower < 1 < upper}); it is also dual-Newton solved and \code{mode = "exact"}
only. Use \code{"logit"} when capping extreme weights is a hard requirement.}

\item{lambda}{Positive soft-constraint penalty. Larger values emphasize
target matching more strongly.}

\item{new_weight}{Name of the calibrated weight column added to \code{data}.}

\item{verbose}{Logical; passed to OSQP.}
}
\value{
An object of class \code{pass_rate_calibration}.
}
\description{
Adjust initial positive weights so that an overall binary outcome rate and
subgroup outcome rates approach or exactly match specified targets. The
optimization is performed on demographic-cell-by-outcome aggregates.
}
\examples{
d <- example_rate_data(300)
targets <- make_rate_targets(groups = list(sex = c(M = 0.72, F = 0.68)))
fit <- calibrate_pass_rates(d, "qualified", "initial_weight",
                            group_vars = "sex", targets = targets)
fit$target_check
}
