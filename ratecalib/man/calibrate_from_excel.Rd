% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{calibrate_from_excel}
\alias{calibrate_from_excel}
\title{Calibrate directly from an Excel workbook}
\usage{
calibrate_from_excel(
  path,
  outcome,
  weight,
  data_sheet = 1,
  targets_sheet = "targets",
  group_vars = NULL,
  ...
)
}
\arguments{
\item{path}{Path to an \code{.xlsx} file.}

\item{outcome}{Name of the binary outcome column.}

\item{weight}{Name of the initial weight column.}

\item{data_sheet}{Sheet holding the sample data (default 1).}

\item{targets_sheet}{Sheet holding the target table (default \code{"targets"}).}

\item{group_vars}{Optional grouping-variable names; inferred from the target
table when \code{NULL}.}

\item{...}{Further arguments passed to \code{\link[=calibrate_pass_rates]{calibrate_pass_rates()}}.}
}
\value{
An object of class \code{pass_rate_calibration}.
}
\description{
One-step convenience: read the sample data and the target table from an
Excel workbook, infer the grouping variables from the targets, and solve.
}
\examples{
if (requireNamespace("openxlsx", quietly = TRUE)) {
  path <- tempfile(fileext = ".xlsx")
  d <- example_rate_data(300)
  tg <- make_rate_targets(groups = list(sex = c(M = 0.72, F = 0.68)))
  openxlsx::write.xlsx(list(data = d, targets = tg), path)
  fit <- calibrate_from_excel(path, "qualified", "initial_weight",
                              data_sheet = "data", targets_sheet = "targets")
  fit$target_check
}
}
