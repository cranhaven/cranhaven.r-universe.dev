% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/easy.R
\name{calibrate_rates}
\alias{calibrate_rates}
\title{One-step pass-rate weight calibration}
\usage{
calibrate_rates(
  data,
  outcome,
  weight,
  overall = NULL,
  groups = list(),
  priority = 5,
  group_priority = 1,
  lower = 0.25,
  upper = 4,
  mode = c("soft", "exact"),
  distance = c("chi2", "raking", "logit"),
  lambda = 10000,
  new_weight = "weight_calibrated",
  check = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame with one row per sampled unit.}

\item{outcome}{Name of the binary outcome column (1 = pass, 0 = fail).}

\item{weight}{Name of the initial weight column.}

\item{overall}{Optional scalar overall target rate; may be \code{NULL}.}

\item{groups}{Named list. Each element name is a grouping variable and each
element is a named numeric vector of target rates.}

\item{priority}{Priority of the overall target. Defaults to 5.}

\item{group_priority}{Priority of the group targets; a single number or a
vector named by grouping variable.}

\item{lower, upper}{Lower and upper bounds on the weight-adjustment multiplier.}

\item{mode}{\code{"soft"} for soft constraints, \code{"exact"} for exact constraints.}

\item{distance}{Calibration distance, passed to \code{\link[=calibrate_pass_rates]{calibrate_pass_rates()}}.
\code{"chi2"} (default), \code{"raking"} (entropy) or \code{"logit"} (bounded); the latter
two are exact mode only.}

\item{lambda}{Soft-constraint penalty strength.}

\item{new_weight}{Name of the new calibrated weight column.}

\item{check}{Whether to run the data checks before solving.}

\item{verbose}{Whether to print OSQP solver information.}
}
\value{
An object of class \code{pass_rate_calibration}.
}
\description{
A convenience interface for everyday use. The user only supplies the data,
the outcome variable, the initial weights, an overall target and per-group
targets; the function builds the target table, identifies grouping
variables, runs the data checks and calls \code{\link[=calibrate_pass_rates]{calibrate_pass_rates()}}.
}
\examples{
d <- example_rate_data(300)
fit <- calibrate_rates(d, "qualified", "initial_weight",
                       groups = list(sex = c(M = 0.72, F = 0.68)))
summary(fit)
}
