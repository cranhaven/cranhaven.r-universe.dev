% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/welfare.R
\name{get_incapacity_caseloads}
\alias{get_incapacity_caseloads}
\title{Get incapacity benefit caseloads}
\usage{
get_incapacity_caseloads(refresh = FALSE)
}
\arguments{
\item{refresh}{Logical. If \code{TRUE}, re-download even if a cached copy
exists. Defaults to \code{FALSE}.}
}
\value{
An \code{obr_tbl} with the standard v0.4.0 schema. The two series
(\code{"Claimants"} and \code{"Share of working age population"}) carry different
units: claimants are in thousands and the share is a percentage. After
calling, the caller may want to overwrite \code{unit} to \code{"count_k"} for the
claimants series, since the heuristic classifier cannot infer the
"thousands" denomination from the series name alone.
}
\description{
Downloads (and caches) the OBR Welfare Trends Report charts and tables
workbook and returns the combined incapacity benefit caseload since
2008-09, in both absolute terms (thousands of claimants) and as a share
of the working-age population.
}
\examples{
\donttest{
op <- options(obr.cache_dir = tempdir())
cases <- get_incapacity_caseloads()
cases[cases$series == "Claimants", ]
options(op)
}

}
\seealso{
Other welfare: 
\code{\link{get_incapacity_spending}()},
\code{\link{get_welfare_spending}()}
}
\concept{welfare}
