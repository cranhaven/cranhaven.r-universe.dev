# Export everything
# exportPattern("^[^\\.]")

export(LCAvarsel,
       print.LCAvarsel,
       fitLCA,
       print.fitLCA,
       controlLCA,
       controlReg,
       controlGA,
       compareCluster,
       maxG)

import("poLCA")

importFrom("stats", formula, na.exclude, var)
importFrom("utils", capture.output, flush.console)

S3method("print", "LCAvarsel")
S3method("print", "fitLCA")