# 0.1.5

- Added '3.2' to 'r_versions' argument in `release_package()`

# 0.1.4

- Changes to DESCRIPTION file following feedback provided by Prof. Ripley.

# 0.1.3

- Fixed call write_PACKAGE() from release_package() which wasn't passing through type parameter which was necessary when on Windows and writing a source package.

# 0.1.2

- Fixed call to full_repo_dir() from release_package() which wasn't passing the `type` parameter in the case when `type` was "src"

# 0.1.1

- UPDATE: added '3.1' to the `r_versions` argument in `release_package()`

# 0.1

- Initial release
