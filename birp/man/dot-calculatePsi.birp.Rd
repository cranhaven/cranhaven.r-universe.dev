% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/birp.R
\name{.calculatePsi.birp}
\alias{.calculatePsi.birp}
\title{Compute step-change indicator matrix for Delta}
\usage{
.calculatePsi.birp(eval_times, times_of_change)
}
\arguments{
\item{eval_times}{Numeric vector; times at which to evaluate the indicator.}

\item{times_of_change}{Numeric vector; the step-change times \eqn{T_1, \ldots, T_{M-1}}.}
}
\value{
A matrix with \code{length(eval_times)} rows and \code{length(times_of_change)} columns.
}
\description{
For a set of evaluation times and a set of step-change times, computes a matrix
where entry \code{[i, m]} is 1 if observation time \code{i} is greater than or equal
to step-change time \code{m}, and 0 otherwise. Used to accumulate the step-change
contributions \eqn{\sum_m \mathbb{1}_{t_k \geq T_m} \Delta(g, m)}.
}
\keyword{internal}
