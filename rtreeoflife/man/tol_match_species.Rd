% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/species.R
\name{tol_match_species}
\alias{tol_match_species}
\title{Match requested species names against the Kew Tree of Life index}
\usage{
tol_match_species(
  species,
  index = tol_species_index(),
  fuzzy = FALSE,
  max_distance = NULL,
  multiple = c("all", "best"),
  ignore_case = TRUE
)
}
\arguments{
\item{species}{Character vector of requested scientific names.}

\item{index}{A species index returned by \code{\link[=tol_species_index]{tol_species_index()}}. If omitted,
the built-in \link{tol_species} dataset is used.}

\item{fuzzy}{If \code{TRUE}, return the closest available name when no exact match
is found.}

\item{max_distance}{Maximum edit distance for fuzzy matching. If \code{NULL}, a
conservative threshold is computed from each requested name.}

\item{multiple}{How to handle multiple records for the same matched species.
\code{"all"} returns all records; \code{"best"} keeps the record with the highest
\code{no_of_genes_recovered} and then \code{no_of_bp_recovered}.}

\item{ignore_case}{Ignore case when matching names.}
}
\value{
A tibble with requested names, match status, matched records, and
FASTA URLs where available.
}
\description{
Match requested species names against the Kew Tree of Life index
}
