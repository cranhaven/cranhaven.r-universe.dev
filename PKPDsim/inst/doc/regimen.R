## ----load-lib, echo=FALSE-----------------------------------------------------
library("PKPDsim")

## ----reg1---------------------------------------------------------------------
reg1 <- new_regimen(
  amt = 100,
  interval = 12,
  n = 5
)

## ----reg2---------------------------------------------------------------------
reg1 <- new_regimen(
  amt = 100,
  times = c(0, 12, 24, 36, 48)
)

## ----reg3---------------------------------------------------------------------
reg1 <- new_regimen(
  amt = c(100, 100, 100, 100, 100),
  times = c(0, 12, 24, 36, 48)
)

## ----reg-infusion-------------------------------------------------------------
reg1 <- new_regimen(
  amt = 100,
  times = c(0, 12, 24, 36, 48),
  type = "infusion",
  t_inf = c(1, 1, 1.5, 1.5, 1)
)

## ----regimen-cmt--------------------------------------------------------------
reg1 <- new_regimen(
  amt = 100,
  times = c(0, 12, 24, 36, 48),
  cmt = c(1, 1, 2, 2, 3)
)

## ----reg_ss-------------------------------------------------------------------
reg_ss <- new_regimen(
  amt = 100,
  interval = 12,
  n = 5,
  ss = TRUE,
  n_ss = 10
)
reg_ss

