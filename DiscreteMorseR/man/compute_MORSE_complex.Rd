% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DiscreteMorseR.R
\name{compute_MORSE_complex}
\alias{compute_MORSE_complex}
\title{Compute Morse complex from mesh}
\usage{
compute_MORSE_complex(
  mesh,
  output_dir = NULL,
  parallel = TRUE,
  cores = 4,
  batch_size = NULL
)
}
\arguments{
\item{mesh}{Mesh object from get_CCMESH()}

\item{output_dir}{Optional directory to save results. If provided, writes:
- vertices.txt, edges.txt, faces.txt: Mesh simplices
- vector_field.txt: Gradient vector field pairs  
- critical_simplices.txt: Critical simplices
- lowerSTAR.txt: Lower star filtration data}

\item{parallel}{Whether to use parallel processing (default: TRUE)}

\item{cores}{Number of cores for parallel processing (default: 4)}

\item{batch_size}{Number of vertices per batch in parallel processing}
}
\value{
A list with three components:
  \item{vector_field}{Character vector of gradient pairs in format "from:to"}
  \item{critical}{Character vector of critical simplices (vertices, edges, faces)}
  \item{simplices}{List containing vertices, edges, and faces data frames}
}
\description{
Compute Morse complex from mesh
}
\examples{
# Create a tetrahedron mesh
vertices <- matrix(c(0,0,0, 1,0,0, 0,1,0, 0,0,1), ncol=3, byrow=TRUE)
colnames(vertices) <- c("X", "Y", "Z")
faces <- matrix(c(1,2,3, 1,2,4, 1,3,4, 2,3,4), ncol=3, byrow=TRUE)
colnames(faces) <- c("i1", "i2", "i3")

# Extract unique edges from faces
all_edges <- rbind(faces[,c(1,2)], faces[,c(1,3)], faces[,c(2,3)])
unique_edges <- unique(t(apply(all_edges, 1, sort)))
edges <- data.frame(i1 = unique_edges[,1], i2 = unique_edges[,2])

# Create mesh object
mesh <- list(vertices = vertices, faces = faces, edges = edges)
attr(mesh, "input_truth") <- 1:nrow(vertices)

# Compute Morse complex (sequential mode for CRAN checks)
result <- compute_MORSE_complex(mesh, parallel = FALSE)

# View results
print(paste("Critical simplices:", length(result$critical)))
print(paste("Gradient pairs:", length(result$vector_field)))
}
