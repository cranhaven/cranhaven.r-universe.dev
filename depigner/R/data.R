#' Data for example
#'
#' This data are imported from the package vcd, which depends on a
#' package (i.e., mvtnorm) which require R >= 3.5. For this reason
#' we do not import the package vcd, but only this dataset. Which is
#' used only in the examples and tests.
#'
#' @format A data.frame with 84 obs. and 5 var.
"Arthritis"
