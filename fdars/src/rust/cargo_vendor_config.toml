# Cargo configuration for vendored dependencies
# This file is copied to .cargo/config.toml during build
# (stored as cargo_vendor_config.toml to avoid R CMD check NOTE about hidden files)

[source.crates-io]
replace-with = "vendored-sources"

[source.vendored-sources]
directory = "vendor"
