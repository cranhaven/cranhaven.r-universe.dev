% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{outliers.depth.trim}
\alias{outliers.depth.trim}
\title{Outlier Detection using Trimmed Depth}
\usage{
outliers.depth.trim(fdataobj, trim = 0.1, dfunc = depth.mode, ...)
}
\arguments{
\item{fdataobj}{An object of class 'fdata'.}

\item{trim}{Proportion of curves to consider as potential outliers.
Default is 0.1 (curves with depth in bottom 10\%).}

\item{dfunc}{Depth function to use. Default is depth.mode.}

\item{...}{Additional arguments passed to depth function.}
}
\value{
A list of class 'outliers.fdata' with components:
\describe{
\item{outliers}{Indices of detected outliers}
\item{depths}{Depth values for all curves}
\item{cutoff}{Depth cutoff used}
}
}
\description{
Detects outliers based on depth trimming.
}
\examples{
fd <- fdata(matrix(rnorm(200), 20, 10))
out <- outliers.depth.trim(fd, trim = 0.1)
}
