% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{outliers.boxplot}
\alias{outliers.boxplot}
\title{Outlier Detection using Functional Boxplot}
\usage{
outliers.boxplot(
  fdataobj,
  prob = 0.5,
  factor = 1.5,
  depth.func = depth.MBD,
  ...
)
}
\arguments{
\item{fdataobj}{An object of class 'fdata'.}

\item{prob}{Proportion of curves for the central region (default 0.5).}

\item{factor}{Factor for fence calculation (default 1.5).}

\item{depth.func}{Depth function to use. Default is depth.MBD.}

\item{...}{Additional arguments passed to depth function.}
}
\value{
A list of class 'outliers.fdata' with components:
\describe{
\item{outliers}{Indices of detected outliers}
\item{depths}{Depth values for all curves}
\item{cutoff}{Not used (for compatibility)}
\item{fdataobj}{Original fdata object}
}
}
\description{
Detects outliers based on the functional boxplot method.
Curves that exceed the fence (1.5 times the central envelope width)
at any point are flagged as outliers.
}
\examples{
# Create functional data with outliers
set.seed(42)
t <- seq(0, 1, length.out = 50)
X <- matrix(0, 30, 50)
for (i in 1:28) X[i, ] <- sin(2*pi*t) + rnorm(50, sd = 0.2)
X[29, ] <- sin(2*pi*t) + 2  # Magnitude outlier
X[30, ] <- cos(2*pi*t)       # Shape outlier
fd <- fdata(X, argvals = t)

# Detect outliers
out <- outliers.boxplot(fd)
print(out)
}
\seealso{
\code{\link{boxplot.fdata}} for functional boxplot visualization
}
