% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simMultiFunData}
\alias{simMultiFunData}
\title{Simulate Multivariate Functional Data}
\usage{
simMultiFunData(
  n,
  argvals,
  M,
  eFun.type = "Fourier",
  eVal.type = "linear",
  mean = NULL,
  seed = NULL
)
}
\arguments{
\item{n}{Number of multivariate curves to generate.}

\item{argvals}{List of numeric vectors, one per component.}

\item{M}{Integer or integer vector. Number of basis functions per component.
If a single integer, used for all components.}

\item{eFun.type}{Character or character vector specifying eigenfunction
type for each component. See \code{\link{eFun}} for options.}

\item{eVal.type}{Character or character vector specifying eigenvalue
decay for each component. See \code{\link{eVal}} for options.}

\item{mean}{List of mean functions (one per component), or \code{NULL}.}

\item{seed}{Optional integer random seed.}
}
\value{
A list of class \code{multiFunData} containing:
\describe{
\item{components}{List of \code{fdata} objects, one per component}
\item{n}{Number of observations}
\item{p}{Number of components}
}
}
\description{
Generates multivariate (vector-valued) functional data where each
component is simulated via Karhunen-Loeve expansion with potentially
different eigenfunctions, eigenvalues, and domains.
}
\examples{
# Two-component multivariate functional data
t1 <- seq(0, 1, length.out = 100)
t2 <- seq(0, 0.5, length.out = 50)

mfd <- simMultiFunData(
  n = 20,
  argvals = list(t1, t2),
  M = c(5, 3),
  eFun.type = c("Fourier", "Wiener"),
  eVal.type = c("exponential", "linear")
)

# Plot first component
plot(mfd$components[[1]], main = "Component 1")
}
\seealso{
\code{\link{simFunData}}, \code{\link{eFun}}, \code{\link{eVal}}
}
