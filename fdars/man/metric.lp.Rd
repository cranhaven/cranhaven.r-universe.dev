% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irregFdata.R, R/metric.R
\name{metric.lp.irregFdata}
\alias{metric.lp.irregFdata}
\alias{metric.lp}
\alias{metric.lp.fdata}
\title{Lp Metric for Functional Data}
\usage{
\method{metric.lp}{irregFdata}(x, p = 2, ...)

metric.lp(x, ...)

\method{metric.lp}{fdata}(x, y = NULL, p = 2, w = 1, ...)
}
\arguments{
\item{x}{A functional data object (\code{fdata} or \code{irregFdata}).}

\item{p}{The order of the Lp metric (default 2 for L2 distance).}

\item{...}{Additional arguments passed to methods.}

\item{y}{An object of class 'fdata'. If NULL, computes self-distances
for x (more efficient symmetric computation). Only supported for \code{fdata}.}

\item{w}{Optional weight vector of length equal to number of evaluation
points. Default is uniform weighting. Only supported for \code{fdata}.}
}
\value{
A distance matrix.
}
\description{
Computes the Lp distance between functional data objects using
numerical integration (Simpson's rule). Works with both regular \code{fdata}
and irregular \code{irregFdata} objects.
}
\examples{
# Regular fdata
fd <- fdata(matrix(rnorm(100), 10, 10))
D <- metric.lp(fd)  # Self-distances

# Irregular fdata
ifd <- sparsify(fd, minObs = 3, maxObs = 7, seed = 42)
D_irreg <- metric.lp(ifd)
}
