% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric.R
\name{semimetric.basis}
\alias{semimetric.basis}
\title{Semi-metric based on Basis Expansion}
\usage{
semimetric.basis(
  fdataobj,
  fdataref = NULL,
  nbasis = 5,
  basis = "bspline",
  nderiv = 0,
  ...
)
}
\arguments{
\item{fdataobj}{An object of class 'fdata'.}

\item{fdataref}{An object of class 'fdata'. If NULL, uses fdataobj.}

\item{nbasis}{Number of basis functions. Default is 5.}

\item{basis}{Type of basis: "bspline" (default) or "fourier".}

\item{nderiv}{Derivative order to compute distance on (default 0).}

\item{...}{Additional arguments (ignored).}
}
\value{
A distance matrix based on basis coefficients.
}
\description{
Computes a semi-metric based on the L2 distance of basis expansion
coefficients. Supports B-spline and Fourier basis.
}
\examples{
# Create curves
t <- seq(0, 1, length.out = 100)
X <- matrix(0, 10, 100)
for (i in 1:10) X[i, ] <- sin(2*pi*t + i/5) + rnorm(100, sd = 0.1)
fd <- fdata(X, argvals = t)

# Compute distance based on B-spline coefficients
D <- semimetric.basis(fd, nbasis = 7)
}
