% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irregFdata.R
\name{as.fdata.irregFdata}
\alias{as.fdata.irregFdata}
\alias{as.fdata}
\alias{as.fdata.fdata}
\title{Convert Irregular Functional Data to Regular Grid}
\usage{
\method{as.fdata}{irregFdata}(x, argvals = NULL, method = c("na", "linear", "nearest"), ...)

as.fdata(x, ...)

\method{as.fdata}{fdata}(x, ...)
}
\arguments{
\item{x}{An object of class \code{irregFdata}.}

\item{argvals}{Target regular grid. If \code{NULL}, uses the union of
all observation points.}

\item{method}{Interpolation method:
\describe{
\item{na}{(Default) Only fill exact matches; other points are \code{NA}}
\item{linear}{Linear interpolation between observed points}
\item{nearest}{Nearest neighbor interpolation}
}}

\item{...}{Additional arguments (ignored).}
}
\value{
An \code{fdata} object with \code{NA} for unobserved points
(unless interpolated).
}
\description{
Creates a regular \code{fdata} object from an \code{irregFdata} object
by interpolating or placing \code{NA} at unobserved points.
}
\examples{
# Create sparse data
t <- seq(0, 1, length.out = 100)
fd <- simFunData(n = 10, argvals = t, M = 5, seed = 42)
ifd <- sparsify(fd, minObs = 10, maxObs = 30, seed = 123)

# Convert back to regular grid with NA
fd_na <- as.fdata(ifd)

# Convert with linear interpolation
fd_interp <- as.fdata(ifd, method = "linear")
}
\seealso{
\code{\link{sparsify}}, \code{\link{irregFdata}}
}
