% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fdata.R
\name{fdata2pls}
\alias{fdata2pls}
\title{Convert Functional Data to PLS Scores}
\usage{
fdata2pls(fdataobj, y, ncomp = 2, lambda = 0, norm = TRUE)
}
\arguments{
\item{fdataobj}{An object of class 'fdata'.}

\item{y}{Response vector (numeric).}

\item{ncomp}{Number of PLS components to extract (default 2).}

\item{lambda}{Regularization parameter (default 0, not currently used).}

\item{norm}{Logical. If TRUE (default), normalize the scores.}
}
\value{
A list with components:
\describe{
\item{weights}{Matrix of PLS weights (m x ncomp)}
\item{scores}{Matrix of PLS scores (n x ncomp)}
\item{loadings}{Matrix of PLS loadings (m x ncomp)}
\item{call}{The function call}
}
}
\description{
Performs Partial Least Squares regression and returns component scores
for functional data using the NIPALS algorithm.
}
\examples{
t <- seq(0, 1, length.out = 50)
X <- matrix(0, 20, 50)
for (i in 1:20) X[i, ] <- sin(2*pi*t) + rnorm(50, sd = 0.1)
y <- rowMeans(X) + rnorm(20, sd = 0.1)
fd <- fdata(X, argvals = t)
pls <- fdata2pls(fd, y, ncomp = 3)
}
